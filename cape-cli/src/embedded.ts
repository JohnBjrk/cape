// Auto-generated by scripts/prebuild.ts — do not edit.
// Run `bun cape-cli/scripts/prebuild.ts` to regenerate.

/** Bundled cape runtime written to user project's node_modules/cape/index.js */
export const CAPE_BUNDLE = "// @bun\nvar __require=import.meta.require;function tokenize(argv){const tokens=[];let pastSeparator=false;for(const arg of argv){if(pastSeparator){tokens.push({type:\"value\",raw:arg});continue}if(arg===\"--\"){tokens.push({type:\"separator\",raw:\"--\"});pastSeparator=true;continue}if(arg.startsWith(\"--\")&&arg.includes(\"=\")){const eq=arg.indexOf(\"=\");tokens.push({type:\"flag\",raw:arg.slice(0,eq)});tokens.push({type:\"value\",raw:arg.slice(eq+1)});continue}if(arg.startsWith(\"--\")||arg.startsWith(\"-\")&&arg.length===2){tokens.push({type:\"flag\",raw:arg});continue}if(arg.startsWith(\"-\")&&arg.length>2&&!arg.startsWith(\"--\")){for(const char of arg.slice(1)){tokens.push({type:\"flag\",raw:`-${char}`})}continue}tokens.push({type:\"value\",raw:arg})}return tokens}class ParseError extends Error{suggestion;constructor(message,suggestion){super(message);this.suggestion=suggestion;this.name=\"ParseError\"}}function resolve(tokens,schema,opts){const schemaFlags=schema.flags??{};const positionalDefs=schema.positionals??[];const aliasMap=new Map;for(const[name,def]of Object.entries(schemaFlags)){if(def.alias)aliasMap.set(`-${def.alias}`,name)}const flags={};const positionals=[];const passthrough=[];const provided=new Set;let pastSeparator=false;let i=0;while(i<tokens.length){const token=tokens[i];if(token.type===\"separator\"){pastSeparator=true;i++;continue}if(pastSeparator||token.type===\"value\"){(pastSeparator?passthrough:positionals).push(token.raw);i++;continue}const raw=token.raw;const canonical=raw.startsWith(\"--\")?raw.slice(2):aliasMap.get(raw);const def=canonical!==undefined?schemaFlags[canonical]:undefined;if(canonical===undefined||def===undefined){const suggestion=closestFlag(raw,Object.keys(schemaFlags));throw new ParseError(`unknown flag ${raw}`,suggestion?`did you mean --${suggestion}?`:undefined)}if(def.type===\"boolean\"){setValue(flags,canonical,true,def.multiple??false);provided.add(canonical);i++;continue}const next=tokens[i+1];if(!next||next.type!==\"value\"){throw new ParseError(`flag --${canonical} requires a value`)}const rawValue=next.raw;if(def.type===\"number\"){const n=Number(rawValue);if(Number.isNaN(n)){throw new ParseError(`--${canonical} expects a number, got \"${rawValue}\"`)}setValue(flags,canonical,n,def.multiple??false)}else{setValue(flags,canonical,rawValue,def.multiple??false)}provided.add(canonical);i+=2}for(const[name,def]of Object.entries(schemaFlags)){if(provided.has(name))continue;if(def.default!==undefined){flags[name]=def.default}else if(def.multiple){flags[name]=[]}else if(def.type===\"boolean\"){flags[name]=false}}if(!opts?.skipRequired){for(const[name,def]of Object.entries(schemaFlags)){if(def.required&&def.type!==\"boolean\"&&!provided.has(name)){throw new ParseError(`missing required flag --${name}`)}}for(let j=0;j<positionalDefs.length;j++){const posDef=positionalDefs[j];if(!posDef.variadic&&positionals[j]===undefined){throw new ParseError(`missing required argument <${posDef.name}>`)}}}return{flags,positionals,passthrough,provided}}function setValue(flags,name,value,multiple){if(multiple){if(!Array.isArray(flags[name]))flags[name]=[];flags[name].push(value)}else{flags[name]=value}}function editDistance(a,b){const m=a.length;const n=b.length;const dp=Array.from({length:m+1},(_,i)=>Array.from({length:n+1},(_2,j)=>i===0?j:j===0?i:0));for(let r=1;r<=m;r++){for(let c=1;c<=n;c++){dp[r][c]=a[r-1]===b[c-1]?dp[r-1][c-1]:1+Math.min(dp[r-1][c],dp[r][c-1],dp[r-1][c-1])}}return dp[m][n]}function closestFlag(raw,names){const input=raw.replace(/^-+/,\"\");let best;let bestDist=Infinity;for(const name of names){const d=editDistance(input,name);if(d<bestDist){bestDist=d;best=name}}return bestDist<=2?best:undefined}var globalSchema={flags:{help:{type:\"boolean\",alias:\"h\",description:\"Show help\"},version:{type:\"boolean\",description:\"Show version\"},json:{type:\"boolean\",description:\"Output as JSON\"},\"no-color\":{type:\"boolean\",description:\"Disable ANSI color and formatting\"},quiet:{type:\"boolean\",alias:\"q\",description:\"Suppress all output except errors\"},verbose:{type:\"boolean\",alias:\"v\",description:\"Enable verbose log output\"},debug:{type:\"boolean\",description:\"Enable debug log output (superset of --verbose)\"},config:{type:\"string\",description:\"Override config file location\"}}};function mergeSchemas(base,overlay){return{flags:{...base.flags,...overlay.flags},positionals:overlay.positionals}}function extractGlobalFlags(parsed){const f=parsed.flags;const debug=Boolean(f[\"debug\"]);return{help:Boolean(f[\"help\"]),version:Boolean(f[\"version\"]),json:Boolean(f[\"json\"]),noColor:Boolean(f[\"no-color\"]),quiet:Boolean(f[\"quiet\"]),verbose:debug||Boolean(f[\"verbose\"]),debug,config:f[\"config\"]}}class HttpError extends Error{status;statusText;url;body;constructor(status,statusText,url,body){super(`HTTP ${status} ${statusText} \\u2014 ${url}`);this.status=status;this.statusText=statusText;this.url=url;this.body=body;this.name=\"HttpError\"}}function createHttp(signal){function rawFetch(url,init){return fetch(url,{...init,signal})}async function jsonRequest(method,url,body,opts){const headers={Accept:\"application/json\",...opts?.headers};let bodyPayload;if(body!==undefined){headers[\"Content-Type\"]=\"application/json\";bodyPayload=JSON.stringify(body)}const res=await rawFetch(url,{method,headers,body:bodyPayload});if(!res.ok){const text2=await res.text().catch(()=>\"\");throw new HttpError(res.status,res.statusText,url,text2)}const contentLength=res.headers.get(\"content-length\");if(res.status===204||contentLength===\"0\")return{};const contentType=res.headers.get(\"content-type\")??\"\";if(contentType.includes(\"application/json\"))return res.json();const text=await res.text();if(!text)return{};try{return JSON.parse(text)}catch{return{}}}return{get:(url,opts)=>jsonRequest(\"GET\",url,undefined,opts),post:(url,body,opts)=>jsonRequest(\"POST\",url,body,opts),put:(url,body,opts)=>jsonRequest(\"PUT\",url,body,opts),patch:(url,body,opts)=>jsonRequest(\"PATCH\",url,body,opts),delete:(url,opts)=>jsonRequest(\"DELETE\",url,undefined,opts),fetch:rawFetch,HttpError}}function createMockHttp(){const calls=[];const mocked=new Map;function resolve2(method,url,body){calls.push({method,url,body});if(mocked.has(url))return Promise.resolve(mocked.get(url));return Promise.reject(new HttpError(404,\"Not Found\",url,`No mock configured for ${method} ${url}`))}return{get:(url)=>resolve2(\"GET\",url),post:(url,body)=>resolve2(\"POST\",url,body),put:(url,body)=>resolve2(\"PUT\",url,body),patch:(url,body)=>resolve2(\"PATCH\",url,body),delete:(url)=>resolve2(\"DELETE\",url),fetch:(url,init)=>{calls.push({method:init?.method??\"GET\",url});return Promise.reject(new Error(\"Use mockJson() for fetch mocking\"))},HttpError,calls,mockJson(url,response){mocked.set(url,response)}}}var E=\"\\x1B[\";var cursor={up:(n=1)=>`${E}${n}A`,down:(n=1)=>`${E}${n}B`,col:(n)=>`${E}${n}G`,hide:`${E}?25l`,show:`${E}?25h`};var erase={line:`${E}2K`,down:`${E}J`};var style={bold:(s)=>`${E}1m${s}${E}0m`,dim:(s)=>`${E}2m${s}${E}0m`,cyan:(s)=>`${E}36m${s}${E}0m`,green:(s)=>`${E}32m${s}${E}0m`,red:(s)=>`${E}31m${s}${E}0m`,yellow:(s)=>`${E}33m${s}${E}0m`,reset:`${E}0m`};function clearAbove(lines){const moveUp=lines>0?cursor.up(lines):\"\";return moveUp+\"\\r\"+erase.down}function countLines(s){let n=0;for(const ch of s)if(ch===`\n`)n++;return n}function createJsonOutput(quiet){const texts=[];const results=[];function addText(text){if(!quiet)texts.push(text)}function addResult(value){results.push(value)}const silentSpinner={update(){},stop(){},succeed(msg){if(msg)addText(msg)},fail(msg){if(msg)addText(msg)}};const silentBar={tick(){},setTotal(){},done(msg){if(msg)addText(msg)}};return{print(text){addText(text)},printError(text){process.stderr.write(text+`\n`)},success(msg){addText(`\\u2713 ${msg}`)},warn(msg){process.stderr.write(`\\u26A0 ${msg}\n`)},json(value){addResult(value)},table(rows){addResult(rows)},list(items){addResult(items)},spinner(){return silentSpinner},async withSpinner(_msg,fn){return fn(silentSpinner)},progressBar(){return silentBar},async withProgressBar(_total,fn){return fn(()=>{})},flushJson(){const hasTexts=texts.length>0;const hasResults=results.length>0;let value;if(!hasTexts&&hasResults){value=results.length===1?results[0]:{results}}else if(hasTexts&&!hasResults){value={output:texts}}else if(hasTexts&&hasResults){value={output:texts,results}}else{value={}}process.stdout.write(JSON.stringify(value,null,2)+`\n`)}}}var SPINNER_FRAMES=[\"\\u280B\",\"\\u2819\",\"\\u2839\",\"\\u2838\",\"\\u283C\",\"\\u2834\",\"\\u2826\",\"\\u2827\",\"\\u2807\",\"\\u280F\"];var SPINNER_MS=80;var PROGRESS_WIDTH=28;function createOutput(opts){const{noColor,quiet,isTTY}=opts;const c=noColor?{bold:(s)=>s,dim:(s)=>s,cyan:(s)=>s,green:(s)=>s,red:(s)=>s,yellow:(s)=>s}:style;function print(text){process.stdout.write(text+`\n`)}function printError(text){process.stderr.write(text+`\n`)}function success(message){if(quiet)return;print(`${c.green(\"\\u2713\")} ${message}`)}function warn(message){printError(`${c.yellow(\"\\u26A0\")} ${message}`)}function jsonOut(value){print(JSON.stringify(value,null,2))}function table(rows,tableOpts){if(rows.length===0)return;const columns=tableOpts?.columns??Object.keys(rows[0]);if(columns.length===0)return;if(!isTTY){print(columns.join(\"\\t\"));for(const row of rows){print(columns.map((col)=>String(row[col]??\"\")).join(\"\\t\"))}return}const widths=columns.map((col)=>{const headerLen=col.length;const maxDataLen=Math.max(...rows.map((r)=>String(r[col]??\"\").length));return Math.max(headerLen,maxDataLen)});const top=\"\\u250C\"+widths.map((w)=>\"\\u2500\".repeat(w+2)).join(\"\\u252C\")+\"\\u2510\";const mid=\"\\u251C\"+widths.map((w)=>\"\\u2500\".repeat(w+2)).join(\"\\u253C\")+\"\\u2524\";const bottom=\"\\u2514\"+widths.map((w)=>\"\\u2500\".repeat(w+2)).join(\"\\u2534\")+\"\\u2518\";const header=\"\\u2502\"+columns.map((col,i)=>` ${c.bold(col.padEnd(widths[i]))} `).join(\"\\u2502\")+\"\\u2502\";print(top);print(header);print(mid);for(const row of rows){const line=\"\\u2502\"+columns.map((col,i)=>` ${String(row[col]??\"\").padEnd(widths[i])} `).join(\"\\u2502\")+\"\\u2502\";print(line)}print(bottom)}function list(items){if(!isTTY){for(const item of items)print(item);return}for(const item of items){print(`  ${c.cyan(\"\\u2022\")} ${item}`)}}function spinner(message){if(!isTTY)return createNoOpSpinner(message,print,printError,noColor);return createRealSpinner(message,noColor)}async function withSpinner(message,fn){const s=spinner(message);try{const result=await fn(s);s.succeed();return result}catch(err){s.fail();throw err}}function progressBar(total){if(!isTTY)return createNoOpProgressBar();return createRealProgressBar(total,noColor)}async function withProgressBar(total,fn){const bar=progressBar(total);try{return await fn((n)=>bar.tick(n))}finally{bar.done()}}return{print,printError,success,warn,json:jsonOut,table,list,spinner,withSpinner,progressBar,withProgressBar}}function createRealSpinner(initialMessage,noColor){let message=initialMessage;let frame=0;let stopped=false;const c=noColor?(s)=>s:style.cyan;process.stdout.write(cursor.hide);const timer=setInterval(()=>{if(stopped)return;const f=SPINNER_FRAMES[frame%SPINNER_FRAMES.length];frame++;process.stdout.write(`\\r${c(f)} ${message}`)},SPINNER_MS);function stop(){if(stopped)return;stopped=true;clearInterval(timer);process.stdout.write(\"\\r\"+erase.line);process.stdout.write(cursor.show)}return{update(msg){message=msg},stop,succeed(msg){stop();const text=msg??message;const prefix=noColor?\"\\u2713\":style.green(\"\\u2713\");process.stdout.write(`${prefix} ${text}\n`)},fail(msg){stop();const text=msg??message;const prefix=noColor?\"\\u2717\":style.red(\"\\u2717\");process.stderr.write(`${prefix} ${text}\n`)}}}function createNoOpSpinner(message,print,printError,noColor){let current=message;const c=noColor?{green:(s)=>s,red:(s)=>s}:style;return{update(msg){current=msg},stop(){},succeed(msg){print(`${c.green(\"\\u2713\")} ${msg??current}`)},fail(msg){printError(`${c.red(\"\\u2717\")} ${msg??current}`)}}}function createRealProgressBar(initialTotal,noColor){let current=0;let total=initialTotal;function render(){const pct=total>0?current/total:0;const filled=Math.floor(pct*PROGRESS_WIDTH);const arrow=filled<PROGRESS_WIDTH?\">\":\"\";const empty=Math.max(0,PROGRESS_WIDTH-filled-(arrow?1:0));const bar=\"=\".repeat(filled)+arrow+\" \".repeat(empty);const text=`\\r[${bar}] ${current}/${total}`;process.stdout.write(noColor?text:style.cyan(text))}render();return{tick(n=1){current=Math.min(total,current+n);render()},setTotal(n){total=n;render()},done(message){current=total;render();process.stdout.write(`\n`);if(message)process.stdout.write(message+`\n`)}}}function createNoOpProgressBar(){return{tick(){},setTotal(){},done(){}}}function createMockOutput(){const calls=[];const noOpSpinner={update(){},succeed(){},fail(){},stop(){}};const noOpBar={tick(){},setTotal(){},done(){}};return{calls,print(text){calls.push({type:\"print\",text})},printError(text){calls.push({type:\"printError\",text})},success(message){calls.push({type:\"success\",message})},warn(message){calls.push({type:\"warn\",message})},json(value){calls.push({type:\"json\",value})},table(rows,opts){calls.push({type:\"table\",rows,opts})},list(items){calls.push({type:\"list\",items})},spinner(message){calls.push({type:\"spinner\",message});return noOpSpinner},async withSpinner(message,fn){calls.push({type:\"spinner\",message});return fn(noOpSpinner)},progressBar(total){calls.push({type:\"progressBar\",total});return noOpBar},async withProgressBar(total,fn){calls.push({type:\"progressBar\",total});return fn(()=>{})}}}import{join}from\"path\";import{homedir}from\"os\";import{readdir}from\"fs/promises\";function createFs(cliName){const configBase=join(xdgConfigHome(),cliName);const dataBase=join(xdgDataHome(),cliName);const cacheBase=join(xdgCacheHome(),cliName);async function read(path){return Bun.file(path).text()}async function readBytes(path){return new Uint8Array(await Bun.file(path).arrayBuffer())}async function write(path,content,mode=420){const dir=path.slice(0,path.lastIndexOf(\"/\"));if(dir){const{mkdir}=await import(\"fs/promises\");await mkdir(dir,{recursive:true})}await Bun.write(path,content);const{chmod}=await import(\"fs/promises\");await chmod(path,mode)}async function exists(path){return Bun.file(path).exists()}async function list(path){try{return await readdir(path)}catch{return[]}}return{read,readBytes,write,exists,list,configPath(...segments){return join(configBase,...segments)},dataPath(...segments){return join(dataBase,...segments)},cachePath(...segments){return join(cacheBase,...segments)}}}function createMockFs(cliName=\"test\"){const files=new Map;function read(path){const entry=files.get(path);if(entry===undefined)return Promise.reject(new Error(`ENOENT: ${path}`));if(typeof entry===\"string\")return Promise.resolve(entry);return Promise.resolve(new TextDecoder().decode(entry))}function readBytes(path){const entry=files.get(path);if(entry===undefined)return Promise.reject(new Error(`ENOENT: ${path}`));if(entry instanceof Uint8Array)return Promise.resolve(entry);return Promise.resolve(new TextEncoder().encode(entry))}function write(path,content){files.set(path,content);return Promise.resolve()}function exists(path){return Promise.resolve(files.has(path))}function list(path){const prefix=path.endsWith(\"/\")?path:path+\"/\";const entries=[];for(const key of files.keys()){if(key.startsWith(prefix)){const relative=key.slice(prefix.length);if(!relative.includes(\"/\"))entries.push(relative)}}return Promise.resolve(entries)}const configBase=join(xdgConfigHome(),cliName);const dataBase=join(xdgDataHome(),cliName);const cacheBase=join(xdgCacheHome(),cliName);return{files,read,readBytes,write,exists,list,configPath(...segments){return join(configBase,...segments)},dataPath(...segments){return join(dataBase,...segments)},cachePath(...segments){return join(cacheBase,...segments)}}}function xdgConfigHome(){return process.env.XDG_CONFIG_HOME??join(homedir(),\".config\")}function xdgDataHome(){return process.env.XDG_DATA_HOME??join(homedir(),\".local\",\"share\")}function xdgCacheHome(){return process.env.XDG_CACHE_HOME??join(homedir(),\".cache\")}function expandHome(p){if(p===\"~\")return homedir();if(p.startsWith(\"~/\"))return join(homedir(),p.slice(2));return p}function createStdin(){const isTTY=!!process.stdin.isTTY;async function read(){const chunks=[];for await(const chunk of process.stdin){chunks.push(chunk)}return Buffer.concat(chunks).toString(\"utf8\")}async function*lines(){let buffer=\"\";for await(const chunk of process.stdin){buffer+=chunk.toString(\"utf8\");const parts=buffer.split(`\n`);buffer=parts.pop()??\"\";for(const line of parts)yield line}if(buffer)yield buffer}return{isTTY,read,lines}}function createMockStdin(content=\"\",isTTY=false){return{isTTY,async read(){return content},async*lines(){for(const line of content.split(`\n`)){if(line||content.endsWith(`\n`))yield line}}}}function createLog(opts){const{noColor}=opts;const isVerbose=opts.verbose||opts.debug;const isDebug=opts.debug;const dim=noColor?(s)=>s:style.dim;return{verbose(message){if(!isVerbose)return;process.stderr.write(dim(`[verbose] ${message}`)+`\n`)},debug(message){if(!isDebug)return;process.stderr.write(dim(`[debug] ${message}`)+`\n`)}}}function createMockLog(){const calls=[];return{calls,verbose(message){calls.push({level:\"verbose\",message})},debug(message){calls.push({level:\"debug\",message})}}}function createSignalManager(){const controller=new AbortController;const handlers=[];async function runHandlers(){for(const fn of[...handlers].reverse()){try{await fn()}catch{}}}const onSIGINT=()=>{controller.abort();runHandlers()};const onSIGTERM=()=>{controller.abort();runHandlers()};process.on(\"SIGINT\",onSIGINT);process.on(\"SIGTERM\",onSIGTERM);return{signal:controller.signal,onExit(fn){handlers.push(fn)},teardown(){process.off(\"SIGINT\",onSIGINT);process.off(\"SIGTERM\",onSIGTERM)}}}function createMockSignalManager(){const controller=new AbortController;const exitHandlers=[];return{signal:controller.signal,exitHandlers,onExit(fn){exitHandlers.push(fn)},teardown(){},abort(){controller.abort()}}}import{join as join2}from\"path\";function parseToml(text){if(!text.trim())return{};return Bun.TOML.parse(text)}function serializeToml(doc){const lines=[];const sections=[];for(const[key,value]of Object.entries(doc)){if(isPlainObject(value)){sections.push([key,value])}else{lines.push(`${key} = ${tomlScalar(value)}`)}}for(const[section,entries]of sections){if(lines.length>0||sections.indexOf([section,entries])>0)lines.push(\"\");lines.push(`[${section}]`);for(const[k,v]of Object.entries(entries)){lines.push(`${k} = ${tomlScalar(v)}`)}}return lines.join(`\n`)+(lines.length>0?`\n`:\"\")}function isPlainObject(v){return v!==null&&typeof v===\"object\"&&!Array.isArray(v)}function tomlScalar(v){if(typeof v===\"string\"){return`\"${v.replace(/\\\\/g,\"\\\\\\\\\").replace(/\"/g,\"\\\\\\\"\").replace(/\\n/g,\"\\\\n\").replace(/\\t/g,\"\\\\t\")}\"`}return String(v)}function createSecrets(cliName,commandName){const filePath=join2(xdgConfigHome(),cliName,\"credentials.toml\");async function loadDoc(){const file=Bun.file(filePath);if(!await file.exists())return{};try{return parseToml(await file.text())}catch{return{}}}async function saveDoc(doc){const{mkdir,chmod}=await import(\"fs/promises\");const dir=filePath.slice(0,filePath.lastIndexOf(\"/\"));await mkdir(dir,{recursive:true});await Bun.write(filePath,serializeToml(doc));await chmod(filePath,384)}return{async get(key){const doc=await loadDoc();const section=doc[commandName]??{};const value=section[key];return typeof value===\"string\"?value:value!==undefined?String(value):undefined},async set(key,value){const doc=await loadDoc();if(!doc[commandName])doc[commandName]={};doc[commandName][key]=value;await saveDoc(doc)},async delete(key){const doc=await loadDoc();const section=doc[commandName];if(section){delete section[key];await saveDoc(doc)}}}}function createMockSecrets(initial={}){const store=new Map(Object.entries(initial));return{store,async get(key){return store.get(key)},async set(key,value){store.set(key,value)},async delete(key){store.delete(key)}}}class NonTtyError extends Error{constructor(message=\"stdin is not a TTY \\u2014 cannot display interactive prompt\"){super(message);this.name=\"NonTtyError\"}}class PromptCancelledError extends Error{constructor(){super(\"Prompt cancelled by user\");this.name=\"PromptCancelledError\"}}class MockRuntime{args;env;output;fs;stdin;log;signal;secrets;config;commandConfig;prompt={text:()=>Promise.reject(new NonTtyError),confirm:()=>Promise.reject(new NonTtyError),select:()=>Promise.reject(new NonTtyError),multiSelect:()=>Promise.reject(new NonTtyError),autocomplete:()=>Promise.reject(new NonTtyError),NonTtyError,PromptCancelledError};http=createMockHttp();printed=[];errors=[];exitCode;get outputCalls(){return this.output.calls}get logCalls(){return this.log.calls}get secretStore(){return this.secrets.store}get fsFiles(){return this.fs.files}_signalManager;constructor(options={}){this.args={flags:{},positionals:[],passthrough:[],provided:new Set,...options.args};this.env=options.env??{};this.config=options.config??{};this.commandConfig=options.commandConfig??{};const mockOutput=createMockOutput();const origPrint=mockOutput.print.bind(mockOutput);const origError=mockOutput.printError.bind(mockOutput);mockOutput.print=(t)=>{this.printed.push(t);origPrint(t)};mockOutput.printError=(t)=>{this.errors.push(t);origError(t)};this.output=mockOutput;const mockFs=createMockFs(options.cliName??\"test\");if(options.files){for(const[path,content]of Object.entries(options.files)){mockFs.files.set(path,content)}}this.fs=mockFs;this.stdin=createMockStdin(options.stdinContent??\"\",options.stdinIsTTY??false);this.log=createMockLog();this.secrets=createMockSecrets(options.secrets??{});this._signalManager=createMockSignalManager();this.signal=this._signalManager.signal}print(text){this.printed.push(text)}printError(text){this.errors.push(text)}exit(code){this.exitCode=code;throw new MockExitError(code)}onExit(fn){this._signalManager.onExit(fn)}async abort(){this._signalManager.abort();for(const fn of[...this._signalManager.exitHandlers].reverse()){try{await fn()}catch{}}}}class MockExitError extends Error{code;constructor(code){super(`Process exited with code ${code}`);this.code=code}}var ESC=\"\\x1B[\";var ansi={bold:(s)=>`${ESC}1m${s}${ESC}0m`,dim:(s)=>`${ESC}2m${s}${ESC}0m`,reset:`${ESC}0m`};function makeStyler(noColor){if(noColor)return{bold:(s)=>s,dim:(s)=>s};return ansi}var INDENT=\"  \";var COL_GAP=3;function renderHelp(cli,ctx,opts={}){const style2=makeStyler(opts.noColor??false);const lines=[];const push=(...ls)=>lines.push(...ls);if(ctx.level===\"root\"){const version=cli.version?` v${cli.version}`:\"\";push(`${style2.bold(cli.name+version)} \\u2014 ${cli.description}`,\"\");push(`${style2.bold(\"Usage:\")} ${cli.name} <command> [subcommand] [flags]`,\"\")}else if(ctx.level===\"command\"){push(`${style2.bold(ctx.command.name)} \\u2014 ${ctx.command.description}`,\"\");const hasSubcmds=ctx.subcommands.length>0;const usageSuffix=hasSubcmds?\"<subcommand> [flags]\":buildPositionalUsage(ctx.command.schema)+\"[flags]\";push(`${style2.bold(\"Usage:\")} ${cli.name} ${ctx.command.name} ${usageSuffix}`,\"\")}else{push(`${style2.bold(`${ctx.command.name} ${ctx.subcommand.name}`)} \\u2014 ${ctx.subcommand.description}`,\"\");push(`${style2.bold(\"Usage:\")} ${cli.name} ${ctx.command.name} ${ctx.subcommand.name} [flags]`,\"\")}if(ctx.level===\"root\"&&ctx.commands.length>0){push(style2.bold(\"Commands:\"));push(...renderSummaries(ctx.commands));push(\"\")}if(ctx.level===\"command\"&&ctx.subcommands.length>0){push(style2.bold(\"Subcommands:\"));push(...renderSummaries(ctx.subcommands));push(\"\")}if(ctx.level===\"root\"){const flagLines=renderFlagSection(globalSchema,style2);if(flagLines.length>0){push(style2.bold(\"Global Flags:\"));push(...flagLines);push(\"\")}}else if(ctx.level===\"command\"){const cmdFlagLines=renderFlagSection(ctx.command.schema,style2,{excludeGlobals:true});if(cmdFlagLines.length>0){push(style2.bold(\"Command Flags:\"));push(...cmdFlagLines);push(\"\")}const globalFlagLines=renderFlagSection(globalSchema,style2);if(globalFlagLines.length>0){push(style2.bold(\"Global Flags:\"));push(...globalFlagLines);push(\"\")}}else{const subFlagLines=renderFlagSection(ctx.subcommand.schema,style2,{excludeGlobals:true});if(subFlagLines.length>0){push(style2.bold(\"Flags:\"));push(...subFlagLines);push(\"\")}const inheritedFlagLines=renderFlagSection(ctx.command.schema,style2,{excludeGlobals:true,excludeHiddenInSubcommand:true});if(inheritedFlagLines.length>0){push(style2.bold(`Inherited from '${ctx.command.name}':`));push(...inheritedFlagLines);push(\"\")}const globalFlagLines=renderFlagSection(globalSchema,style2);if(globalFlagLines.length>0){push(style2.bold(\"Global Flags:\"));push(...globalFlagLines);push(\"\")}}if(ctx.level===\"root\"){push(style2.dim(`Run '${cli.name} <command> --help' for command-specific help.`))}else if(ctx.level===\"command\"&&ctx.subcommands.length>0){push(style2.dim(`Run '${cli.name} ${ctx.command.name} <subcommand> --help' for subcommand-specific help.`))}return lines.join(`\n`)}function buildPositionalUsage(schema){const positionals=schema.positionals??[];if(positionals.length===0)return\"\";return positionals.map((p)=>p.variadic?`[${p.name}...]`:`[${p.name}]`).join(\" \")+\" \"}function renderSummaries(items){const colWidth=Math.max(...items.map((c)=>c.name.length))+COL_GAP;return items.map((c)=>{const nameCol=(c.name+(c.aliases?.length?`, ${c.aliases.join(\", \")}`:\"\")).padEnd(colWidth);return`${INDENT}${nameCol}${c.description}`})}function renderFlagSection(schema,style2,opts={}){const globalFlagNames=new Set(Object.keys(globalSchema.flags??{}));const entries=Object.entries(schema.flags??{}).filter(([name,def])=>{if(opts.excludeGlobals&&globalFlagNames.has(name))return false;if(opts.excludeHiddenInSubcommand&&def.hideInSubcommandHelp)return false;return true});if(entries.length===0)return[];const rows=entries.map(([name,def])=>{const alias=def.alias?`, -${def.alias}`:\"\";const typeHint=def.type!==\"boolean\"?` <${def.type}>`:\"\";const multiple=def.multiple?\"...\":\"\";const left=`--${name}${alias}${typeHint}${multiple}`;const defaultHint=def.default!==undefined?style2.dim(` (default: ${def.default})`):\"\";const right=(def.description??\"\")+defaultHint;return{left,right}});const colWidth=Math.max(...rows.map((r)=>r.left.length))+COL_GAP;return rows.map((r)=>`${INDENT}${r.left.padEnd(colWidth)}${r.right}`)}import{join as join3,dirname}from\"path\";import{readdir as readdir2}from\"fs/promises\";async function discoverPlugins(dirs,opts){const results=[];const seen=new Set;for(const dir of dirs){const manifestPaths=await findManifestFiles(dir);for(const manifestPath of manifestPaths){const manifest=await parseManifest(manifestPath);if(!manifest)continue;if(!manifest.enabled&&!opts?.includeDisabled)continue;if(seen.has(manifest.name))continue;seen.add(manifest.name);results.push({manifest,pluginDir:dirname(manifestPath),manifestPath})}}return results}async function findManifestFiles(dir){const results=[];let entries;try{entries=await readdir2(dir,{withFileTypes:true})}catch{return[]}for(const entry of entries){const fullPath=join3(dir,entry.name);if(entry.isDirectory()){results.push(...await findManifestFiles(fullPath))}else if(entry.name.endsWith(\".plugin.toml\")){results.push(fullPath)}}return results}async function parseManifest(manifestPath){let raw;try{const content=await Bun.file(manifestPath).text();raw=Bun.TOML.parse(content)}catch(err){console.warn(`[cape] Failed to parse plugin manifest at ${manifestPath}: ${err}`);return}if(!isValidManifest(raw)){console.warn(`[cape] Invalid plugin manifest at ${manifestPath}: missing required fields`);return}return raw}function isValidManifest(raw){if(typeof raw!==\"object\"||raw===null)return false;const m=raw;return typeof m[\"name\"]===\"string\"&&typeof m[\"description\"]===\"string\"&&typeof m[\"command\"]===\"string\"&&typeof m[\"enabled\"]===\"boolean\"&&typeof m[\"frameworkVersion\"]===\"string\"}import{join as join4,isAbsolute}from\"path\";var FRAMEWORK_VERSION=\"1.0.0\";async function loadPlugin(plugin,mode){checkCompatibility(plugin);const absPath=resolveCommandPath(plugin);globalThis[\"__CAPE_EXECUTION_MODE__\"]=mode;let mod;try{mod=await import(absPath)}finally{globalThis[\"__CAPE_EXECUTION_MODE__\"]=undefined}const exported=mod[\"default\"]??mod[\"command\"];if(!exported||typeof exported!==\"object\"){throw new Error(`Plugin \"${plugin.manifest.name}\" at ${absPath} has no default export. Export a CommandDef as the default export.`)}return exported}function checkCompatibility(plugin){const pluginMajor=parseMajor(plugin.manifest.frameworkVersion);const frameworkMajor=parseMajor(FRAMEWORK_VERSION);if(pluginMajor===null){throw new Error(`Plugin \"${plugin.manifest.name}\" has an invalid frameworkVersion: \"${plugin.manifest.frameworkVersion}\". Expected semver format (e.g. \"1.0.0\").`)}if(pluginMajor!==frameworkMajor){throw new Error(`Plugin \"${plugin.manifest.name}\" requires framework v${pluginMajor}.x but this binary uses v${FRAMEWORK_VERSION}. Rebuild the plugin against the current framework version.`)}}function parseMajor(version){const major=parseInt(version.split(\".\")[0]??\"\",10);return Number.isNaN(major)?null:major}function resolveCommandPath(plugin){const{command}=plugin.manifest;return isAbsolute(command)?command:join4(plugin.pluginDir,command)}import{join as join5}from\"path\";import{mkdir}from\"fs/promises\";var CACHE_TTL_MS=5*60*1000;async function resolveCompletions(commands,argv,partial,cliName){const slot=computeSlot(commands,argv,partial);return fetchCandidates(slot,partial,commands,cliName)}function computeSlot(commands,argv,partial){const cmdResult=freeValueAt(argv,0,globalSchema);const command=cmdResult?findByName(commands,cmdResult.value):undefined;if(!command||!cmdResult){if(partial.startsWith(\"-\")){return{kind:\"flag-name\",schema:globalSchema,provided:new Set}}return{kind:\"command\"}}const cmdSchema=command.schema??{};const subcommands=command.subcommands??[];const subScanSchema=mergeSchemas(globalSchema,cmdSchema);const subResult=freeValueAt(argv,cmdResult.index+1,subScanSchema);const subcommand=subResult?findByName(subcommands,subResult.value):undefined;const subIdx=subResult&&subcommand?subResult.index:-1;const stripped=argv.filter((_,i)=>i!==cmdResult.index&&i!==subIdx);const subSchema=subcommand?.schema??{};const activeSchema=subcommand?mergeSchemas(mergeSchemas(globalSchema,cmdSchema),subSchema):mergeSchemas(globalSchema,cmdSchema);const{provided,expectingValueFor}=walkArgv(stripped,activeSchema);if(expectingValueFor){const flagDef=lookupFlag(activeSchema,expectingValueFor);return{kind:\"flag-value\",flagName:expectingValueFor,source:flagDef?.complete,ctx:{partial,flags:collectFlags(stripped,activeSchema)}}}if(partial.startsWith(\"-\")){return{kind:\"flag-name\",schema:activeSchema,provided}}if(!subcommand&&subcommands.length>0){return{kind:\"subcommand\",command}}const posIdx=countPositionals(stripped,activeSchema);const posDefs=activeSchema.positionals??[];return{kind:\"positional\",index:posIdx,source:posDefs[posIdx]?.complete,ctx:{partial,flags:collectFlags(stripped,activeSchema)}}}async function fetchCandidates(slot,partial,commands,cliName){switch(slot.kind){case\"command\":return filterPrefix(commands.flatMap((c)=>[c.name,...c.aliases??[]]),partial);case\"subcommand\":return filterPrefix((slot.command.subcommands??[]).flatMap((s)=>[s.name,...s.aliases??[]]),partial);case\"flag-name\":return filterPrefix(flagNameCandidates(slot.schema,slot.provided),partial);case\"flag-value\":return filterPrefix(await fetchSource(slot.source,slot.ctx,`flag:${slot.flagName}`,cliName),partial);case\"positional\":return filterPrefix(await fetchSource(slot.source,slot.ctx,`pos:${slot.index}`,cliName),partial)}}async function fetchSource(source,ctx,slotKey,cliName){if(!source)return[];if(source.type===\"static\")return source.values;const cacheFile=cliName?completionCachePath(cliName,slotKey,ctx):undefined;if(cacheFile){const cached=await readCache(cacheFile);if(cached)return cached}let values;try{values=await withTimeout(source.fetch(ctx),source.timeoutMs??5000)}catch{return[]}if(cacheFile){writeCache(cacheFile,values).catch(()=>{})}return values}function completionCachePath(cliName,slotKey,ctx){const key=`${cliName}:${slotKey}:${JSON.stringify(ctx)}`;const hash=Bun.hash(key).toString(16);return join5(xdgCacheHome(),cliName,\"completions\",`${hash}.json`)}async function readCache(path){const f=Bun.file(path);if(!await f.exists())return;try{const entry=await f.json();return entry.expires>Date.now()?entry.values:undefined}catch{return}}async function writeCache(path,values){await mkdir(join5(path,\"..\"),{recursive:true});const entry={expires:Date.now()+CACHE_TTL_MS,values};await Bun.write(path,JSON.stringify(entry))}function flagNameCandidates(schema,provided){const flags=schema.flags??{};const candidates=[];for(const[name,def]of Object.entries(flags)){if(!def.multiple&&provided.has(name))continue;candidates.push(`--${name}`);if(def.alias)candidates.push(`-${def.alias}`)}return candidates}function walkArgv(argv,schema){const flags=schema.flags??{};const provided=new Set;let expectingValueFor;let i=0;while(i<argv.length){const arg=argv[i];expectingValueFor=undefined;if(arg===\"--\")break;if(!arg.startsWith(\"-\")){i++;continue}const eqIdx=arg.indexOf(\"=\");if(eqIdx!==-1){const name2=canonicalize(arg.slice(0,eqIdx),flags);if(name2)provided.add(name2);i++;continue}const name=canonicalize(arg,flags);if(!name){i++;continue}const def=flags[name];if(!def||def.type===\"boolean\"){provided.add(name);i++;continue}provided.add(name);if(i+1<argv.length){i+=2}else{expectingValueFor=name;i++}}return{provided,expectingValueFor}}function countPositionals(argv,schema){const flags=schema.flags??{};let count=0;let i=0;while(i<argv.length){const arg=argv[i];if(arg===\"--\")break;if(!arg.startsWith(\"-\")){count++;i++;continue}if(arg.includes(\"=\")){i++;continue}const name=canonicalize(arg,flags);const def=name?flags[name]:undefined;i+=def&&def.type!==\"boolean\"?2:1}return count}function collectFlags(argv,schema){const flags=schema.flags??{};const result={};let i=0;while(i<argv.length){const arg=argv[i];if(arg===\"--\")break;if(!arg.startsWith(\"-\")){i++;continue}const eqIdx=arg.indexOf(\"=\");if(eqIdx!==-1){const name2=canonicalize(arg.slice(0,eqIdx),flags);if(name2)result[name2]=arg.slice(eqIdx+1);i++;continue}const name=canonicalize(arg,flags);if(!name){i++;continue}const def=flags[name];if(!def){i++;continue}if(def.type===\"boolean\"){result[name]=true;i++}else if(i+1<argv.length){result[name]=argv[i+1];i+=2}else{i++}}return result}function canonicalize(raw,flags){if(raw.startsWith(\"--\"))return raw.slice(2);if(raw.startsWith(\"-\")&&raw.length===2){for(const[name,def]of Object.entries(flags)){if(def.alias===raw[1])return name}}return}function freeValueAt(argv,startIndex,schema){const flags=schema.flags??{};let i=startIndex;while(i<argv.length){const arg=argv[i];if(arg===\"--\")break;if(!arg.startsWith(\"-\"))return{value:arg,index:i};if(arg.includes(\"=\")){i++;continue}const name=canonicalize(arg,flags);const def=name?flags[name]:undefined;i+=def&&def.type!==\"boolean\"?2:1}return}function findByName(items,name){return items.find((item)=>item.name===name||(item.aliases??[]).includes(name))}function filterPrefix(values,prefix){if(!prefix)return values;return values.filter((v)=>v.startsWith(prefix))}function lookupFlag(schema,canonical){return schema.flags?.[canonical]}function withTimeout(promise,ms){return Promise.race([promise,new Promise((_,reject)=>setTimeout(()=>reject(new Error(\"completion timeout\")),ms))])}function parseKey(buf){if(buf.length===0)return null;const b0=buf[0];if(b0===3||b0===4)return{key:{type:\"interrupt\"},consumed:1};if(b0===13||b0===10)return{key:{type:\"enter\"},consumed:1};if(b0===127||b0===8)return{key:{type:\"backspace\"},consumed:1};if(b0===9)return{key:{type:\"tab\"},consumed:1};if(b0===27){if(buf.length===1)return{key:{type:\"escape\"},consumed:1};if(buf[1]===91){if(buf.length<3)return null;const b2=buf[2];switch(b2){case 65:return{key:{type:\"up\"},consumed:3};case 66:return{key:{type:\"down\"},consumed:3};case 67:return{key:{type:\"right\"},consumed:3};case 68:return{key:{type:\"left\"},consumed:3};case 72:return{key:{type:\"home\"},consumed:3};case 70:return{key:{type:\"end\"},consumed:3};case 51:if(buf.length<4)return null;if(buf[3]===126)return{key:{type:\"delete\"},consumed:4};break;case 49:if(buf.length<4)return null;if(buf[3]===126)return{key:{type:\"home\"},consumed:4};break;case 52:if(buf.length<4)return null;if(buf[3]===126)return{key:{type:\"end\"},consumed:4};break}return{key:{type:\"escape\"},consumed:3}}if(buf[1]===79){if(buf.length<3)return null;const b2=buf[2];switch(b2){case 65:return{key:{type:\"up\"},consumed:3};case 66:return{key:{type:\"down\"},consumed:3};case 67:return{key:{type:\"right\"},consumed:3};case 68:return{key:{type:\"left\"},consumed:3};case 72:return{key:{type:\"home\"},consumed:3};case 70:return{key:{type:\"end\"},consumed:3}}}return{key:{type:\"escape\"},consumed:1}}if(b0>=32&&b0<=126){return{key:{type:\"char\",char:String.fromCharCode(b0)},consumed:1}}return{key:{type:\"escape\"},consumed:1}}function parseKeys(chunk){const buf=Array.from(chunk);const keys=[];let offset=0;while(offset<buf.length){const result=parseKey(buf.slice(offset));if(!result){offset++;continue}keys.push(result.key);offset+=result.consumed}return keys}function makeKeyReader(){const queue=[];let waiting;const handler=(chunk)=>{for(const key of parseKeys(chunk)){if(waiting){const resolve2=waiting;waiting=undefined;resolve2(key)}else{queue.push(key)}}};process.stdin.on(\"data\",handler);return{next:()=>new Promise((resolve2)=>{if(queue.length>0){resolve2(queue.shift())}else{waiting=resolve2}}),cleanup:()=>{process.stdin.removeListener(\"data\",handler);waiting=undefined}}}async function runPromptLoop(render,onKey,signal){if(!process.stdin.isTTY)throw new NonTtyError;process.stdout.write(cursor.hide);process.stdin.setRawMode(true);process.stdin.resume();const{next,cleanup}=makeKeyReader();let linesRendered=-1;const redraw=()=>{if(linesRendered>=0){process.stdout.write(clearAbove(linesRendered))}const output=render();process.stdout.write(output);linesRendered=countLines(output)};try{redraw();while(true){if(signal?.aborted){return\"cancelled\"}const key=await Promise.race([next(),signal?new Promise((_,reject)=>{signal.addEventListener(\"abort\",()=>reject(new Error(\"aborted\")),{once:true})}):new Promise(()=>{})]).catch(()=>({type:\"interrupt\"}));const result=onKey(key);if(result!==\"continue\"){return result}redraw()}}finally{cleanup();if(linesRendered>=0)process.stdout.write(clearAbove(linesRendered));process.stdout.write(cursor.show);process.stdin.setRawMode(false);process.stdin.pause()}}function printAnswer(line){process.stdout.write(line+`\n`)}function handleCancelled(line){process.stdout.write(line+`\n`);throw new PromptCancelledError}function textReducer(state,key,validate){if(state.done||state.cancelled)return state;switch(key.type){case\"interrupt\":case\"escape\":return{...state,cancelled:true};case\"enter\":{const err=validate?.(state.value);if(err)return{...state,error:err};return{...state,isPlaceholder:false,error:undefined,done:true}}case\"char\":if(state.isPlaceholder){return{value:key.char,cursor:1,isPlaceholder:false,error:undefined,done:false,cancelled:false}}return insertChar(state,key.char);case\"backspace\":if(state.isPlaceholder){return{...state,value:\"\",cursor:0,isPlaceholder:false}}return deleteBack(state);case\"delete\":if(state.isPlaceholder){return{...state,value:\"\",cursor:0,isPlaceholder:false}}return deleteForward(state);case\"left\":return{...state,isPlaceholder:false,cursor:Math.max(0,state.cursor-1)};case\"right\":return{...state,isPlaceholder:false,cursor:Math.min(state.value.length,state.cursor+1)};case\"home\":return{...state,isPlaceholder:false,cursor:0};case\"end\":return{...state,isPlaceholder:false,cursor:state.value.length};default:return state}}function insertChar(state,char){const{value,cursor:pos}=state;return{...state,value:value.slice(0,pos)+char+value.slice(pos),cursor:pos+1,error:undefined}}function deleteBack(state){const{value,cursor:pos}=state;if(pos===0)return state;return{...state,value:value.slice(0,pos-1)+value.slice(pos),cursor:pos-1}}function deleteForward(state){const{value,cursor:pos}=state;if(pos===value.length)return state;return{...state,value:value.slice(0,pos)+value.slice(pos+1)}}function renderText(state,opts){if(state.done){return`${style.green(\"\\u2713\")} ${style.bold(opts.message)} ${style.dim(state.value)}`}if(state.cancelled){return`${style.red(\"\\u2717\")} ${style.bold(opts.message)}`}const displayValue=state.isPlaceholder?style.dim(state.value):state.value;const lines=[`${style.cyan(\"?\")} ${style.bold(opts.message)} ${displayValue}`];if(state.error)lines.push(`  ${style.red(state.error)}`);return lines.join(`\n`)}async function text(opts){let state={value:opts.default??\"\",cursor:(opts.default??\"\").length,isPlaceholder:!!opts.default,error:undefined,done:false,cancelled:false};const result=await runPromptLoop(()=>{const line=renderText(state,opts);const inputCol=`? ${opts.message} `.length+state.cursor+1;return line+cursor.col(inputCol)},(key)=>{state=textReducer(state,key,opts.validate);if(state.done)return\"done\";if(state.cancelled)return\"cancelled\";return\"continue\"},opts.signal);if(result===\"cancelled\"){handleCancelled(renderText({...state,cancelled:true},opts))}printAnswer(renderText(state,opts));return state.value}function selectReducer(state,key){if(state.done||state.cancelled)return state;const{choices,index}=state;switch(key.type){case\"interrupt\":case\"escape\":return{...state,cancelled:true};case\"enter\":return{...state,done:true};case\"up\":return{...state,index:(index-1+choices.length)%choices.length};case\"down\":return{...state,index:(index+1)%choices.length};case\"home\":return{...state,index:0};case\"end\":return{...state,index:choices.length-1};case\"char\":{const ch=key.char.toLowerCase();const match=choices.findIndex((c)=>c.toLowerCase().startsWith(ch));if(match!==-1)return{...state,index:match};return state}default:return state}}function renderSelect(state,opts){if(state.done){return`${style.green(\"\\u2713\")} ${style.bold(opts.message)} ${style.dim(state.choices[state.index])}`}if(state.cancelled){return`${style.red(\"\\u2717\")} ${style.bold(opts.message)}`}const lines=[`${style.cyan(\"?\")} ${style.bold(opts.message)} ${style.dim(\"(\\u2191\\u2193 to move, Enter to select)\")}`,...state.choices.map((choice,i)=>i===state.index?`  ${style.cyan(\"\\u276F\")} ${style.bold(choice)}`:`    ${choice}`)];return lines.join(`\n`)}async function select(opts){const defaultIndex=opts.default?Math.max(0,opts.choices.indexOf(opts.default)):0;let state={choices:opts.choices,index:defaultIndex,done:false,cancelled:false};const result=await runPromptLoop(()=>renderSelect(state,opts),(key)=>{state=selectReducer(state,key);if(state.done)return\"done\";if(state.cancelled)return\"cancelled\";return\"continue\"},opts.signal);if(result===\"cancelled\"){handleCancelled(renderSelect({...state,cancelled:true},opts))}printAnswer(renderSelect(state,opts));return state.choices[state.index]}function confirmReducer(state,key,defaultValue){if(state.done||state.cancelled)return state;switch(key.type){case\"interrupt\":case\"escape\":return{...state,cancelled:true};case\"enter\":return{...state,answer:state.answer??defaultValue,done:true};case\"char\":{const ch=key.char.toLowerCase();if(ch===\"y\")return{answer:true,done:true,cancelled:false};if(ch===\"n\")return{answer:false,done:true,cancelled:false};return state}default:return state}}function renderConfirm(state,opts){const hint=opts.default===true?\"Y/n\":\"y/N\";const question=`${style.cyan(\"?\")} ${style.bold(opts.message)} ${style.dim(`(${hint})`)} `;if(state.done){return`${style.green(\"\\u2713\")} ${style.bold(opts.message)} ${style.dim(state.answer?\"yes\":\"no\")}`}if(state.cancelled){return`${style.red(\"\\u2717\")} ${style.bold(opts.message)}`}return question}async function confirm(opts){let state={answer:undefined,done:false,cancelled:false};const defaultValue=opts.default??false;const result=await runPromptLoop(()=>renderConfirm(state,opts),(key)=>{state=confirmReducer(state,key,defaultValue);if(state.done)return\"done\";if(state.cancelled)return\"cancelled\";return\"continue\"},opts.signal);if(result===\"cancelled\"){handleCancelled(renderConfirm({...state,cancelled:true},opts))}printAnswer(renderConfirm(state,opts));return state.answer}function multiSelectReducer(state,key){if(state.done||state.cancelled)return state;const{choices,index,checked}=state;switch(key.type){case\"interrupt\":case\"escape\":return{...state,cancelled:true};case\"enter\":return{...state,done:true};case\"up\":return{...state,index:(index-1+choices.length)%choices.length};case\"down\":return{...state,index:(index+1)%choices.length};case\"home\":return{...state,index:0};case\"end\":return{...state,index:choices.length-1};case\"char\":if(key.char===\" \")return toggleCurrent(state);if(key.char===\"a\")return toggleAll(state);return state;default:return state}}function toggleCurrent(state){const next=new Set(state.checked);if(next.has(state.index))next.delete(state.index);else next.add(state.index);return{...state,checked:next}}function toggleAll(state){if(state.checked.size===state.choices.length){return{...state,checked:new Set}}return{...state,checked:new Set(state.choices.map((_,i)=>i))}}function renderMultiSelect(state,opts){if(state.done){const selected=state.choices.filter((_,i)=>state.checked.has(i));return`${style.green(\"\\u2713\")} ${style.bold(opts.message)} ${style.dim(selected.join(\", \")||\"(none)\")}`}if(state.cancelled){return`${style.red(\"\\u2717\")} ${style.bold(opts.message)}`}const lines=[`${style.cyan(\"?\")} ${style.bold(opts.message)} ${style.dim(\"(\\u2191\\u2193 move, Space toggle, a all, Enter confirm)\")}`,...state.choices.map((choice,i)=>{const checked=state.checked.has(i)?style.green(\"\\u25C9\"):\"\\u25CB\";const label=i===state.index?style.bold(style.cyan(choice)):choice;const pointer=i===state.index?style.cyan(\"\\u276F\"):\" \";return`  ${pointer} ${checked} ${label}`})];return lines.join(`\n`)}async function multiSelect(opts){const defaultChecked=new Set((opts.defaults??[]).map((d)=>opts.choices.indexOf(d)).filter((i)=>i!==-1));let state={choices:opts.choices,index:0,checked:defaultChecked,done:false,cancelled:false};const result=await runPromptLoop(()=>renderMultiSelect(state,opts),(key)=>{state=multiSelectReducer(state,key);if(state.done)return\"done\";if(state.cancelled)return\"cancelled\";return\"continue\"},opts.signal);if(result===\"cancelled\"){handleCancelled(renderMultiSelect({...state,cancelled:true},opts))}printAnswer(renderMultiSelect(state,opts));return state.choices.filter((_,i)=>state.checked.has(i))}function autocompleteReducer(state,action){if(state.done||state.cancelled)return state;if(action.type===\"items\"){return{...state,items:action.items,index:action.items.length>0?0:-1,loading:false}}const{key}=action;switch(key.type){case\"interrupt\":case\"escape\":return{...state,cancelled:true};case\"enter\":{const selected=state.index>=0?state.items[state.index]:state.items[0];const value=selected??(state.query||state.defaultValue)??\"\";return{...state,query:value,done:true}}case\"tab\":{const value=state.index>=0?state.items[state.index]??state.query:state.items[0]??state.query;return{...state,query:value,queryCursor:value.length,index:-1}}case\"up\":return{...state,index:Math.max(-1,state.index-1)};case\"down\":return{...state,index:Math.min(state.items.length-1,state.index+1)};case\"char\":return insertChar2(state,key.char);case\"backspace\":return deleteBack2(state);case\"delete\":return deleteForward2(state);case\"left\":return{...state,queryCursor:Math.max(0,state.queryCursor-1)};case\"right\":return{...state,queryCursor:Math.min(state.query.length,state.queryCursor+1)};case\"home\":return{...state,queryCursor:0};case\"end\":return{...state,queryCursor:state.query.length};default:return state}}function insertChar2(state,char){const{query,queryCursor:pos}=state;const newQuery=query.slice(0,pos)+char+query.slice(pos);return{...state,query:newQuery,queryCursor:pos+1,index:-1,loading:true}}function deleteBack2(state){const{query,queryCursor:pos}=state;if(pos===0)return state;const newQuery=query.slice(0,pos-1)+query.slice(pos);return{...state,query:newQuery,queryCursor:pos-1,index:-1,loading:true}}function deleteForward2(state){const{query,queryCursor:pos}=state;if(pos===query.length)return state;const newQuery=query.slice(0,pos)+query.slice(pos+1);return{...state,query:newQuery,index:-1,loading:true}}var MAX_VISIBLE=8;var SPINNER_FRAMES2=[\"\\u280B\",\"\\u2819\",\"\\u2839\",\"\\u2838\",\"\\u283C\",\"\\u2834\",\"\\u2826\",\"\\u2827\",\"\\u2807\",\"\\u280F\"];function renderAutocomplete(state,opts,spinnerFrame=0){if(state.done){return`${style.green(\"\\u2713\")} ${style.bold(opts.message)} ${style.dim(state.query)}`}if(state.cancelled){return`${style.red(\"\\u2717\")} ${style.bold(opts.message)}`}const prefix=state.loading?style.cyan(SPINNER_FRAMES2[spinnerFrame%SPINNER_FRAMES2.length]):style.cyan(\"?\");const queryDisplay=state.query||(opts.default?style.dim(opts.default):\"\");const inputLine=`${prefix} ${style.bold(opts.message)} ${queryDisplay}`;const hintLine=`  ${style.dim(\"(type to filter, \\u2191\\u2193 navigate, Tab/Enter to select)\")}`;const visible=state.items.slice(0,MAX_VISIBLE);const itemLines=visible.map((item,i)=>{const isHighlighted=i===state.index;return isHighlighted?`  ${style.cyan(\"\\u276F\")} ${style.bold(item)}`:`    ${item}`});if(visible.length===0&&!state.loading){itemLines.push(`    ${style.dim(\"(no matches)\")}`)}else if(state.items.length>MAX_VISIBLE){itemLines.push(style.dim(`    \\u2026 ${state.items.length-MAX_VISIBLE} more`))}return[inputLine,hintLine,...itemLines].join(`\n`)}async function autocomplete(opts){if(!process.stdin.isTTY)throw new NonTtyError;const isStatic=Array.isArray(opts.choices);function floatDefault(items){if(!opts.default)return items;const idx=items.indexOf(opts.default);if(idx<=0)return items;return[items[idx],...items.slice(0,idx),...items.slice(idx+1)]}function filterStatic(query){const choices=opts.choices;if(!query)return floatDefault(choices);const q=query.toLowerCase();return choices.filter((c)=>c.toLowerCase().includes(q))}const initialItems=isStatic?filterStatic(\"\"):[];let state={query:\"\",queryCursor:0,items:initialItems,index:initialItems.length>0?0:-1,loading:!isStatic,done:false,cancelled:false,defaultValue:opts.default};let spinnerFrame=0;let spinnerTimer;function startSpinner(){if(spinnerTimer)return;spinnerTimer=setInterval(()=>{if(!state.loading){clearInterval(spinnerTimer);spinnerTimer=undefined;return}spinnerFrame++;redraw()},80)}let linesRendered=-1;const redraw=()=>{if(linesRendered>=0)process.stdout.write(clearAbove(linesRendered));const output=renderAutocomplete(state,opts,spinnerFrame);const nLines=countLines(output);const inputCol=`? ${opts.message} `.length+state.queryCursor+1;const moveUp=nLines>0?cursor.up(nLines):\"\";process.stdout.write(output+moveUp+cursor.col(inputCol));linesRendered=0};process.stdout.write(cursor.hide);process.stdin.setRawMode(true);process.stdin.resume();const{next:nextKey,cleanup}=makeKeyReader();let fetchController;let fetchTimer;const debounceMs=opts.debounceMs??150;const triggerFetch=(query)=>{if(isStatic){state=autocompleteReducer(state,{type:\"items\",items:filterStatic(query)});redraw();return}clearTimeout(fetchTimer);fetchController?.abort();fetchController=new AbortController;const signal=fetchController.signal;startSpinner();fetchTimer=setTimeout(async()=>{try{const fn=opts.choices;let items=await fn(query,signal);if(!signal.aborted){if(!query)items=floatDefault(items);state=autocompleteReducer(state,{type:\"items\",items});redraw()}}catch{if(!signal.aborted){state=autocompleteReducer(state,{type:\"items\",items:[]});redraw()}}},debounceMs)};try{if(!isStatic)triggerFetch(state.query);redraw();while(!state.done&&!state.cancelled){const key=await nextKey();const prevQuery=state.query;state=autocompleteReducer(state,{type:\"key\",key});if(state.query!==prevQuery){triggerFetch(state.query)}if(!state.done&&!state.cancelled){redraw()}}}finally{clearTimeout(fetchTimer);clearInterval(spinnerTimer);fetchController?.abort();cleanup();if(linesRendered>=0)process.stdout.write(clearAbove(linesRendered));process.stdout.write(cursor.show);process.stdin.setRawMode(false);process.stdin.pause()}if(state.cancelled){process.stdout.write(renderAutocomplete({...state,cancelled:true},opts)+`\n`);throw new PromptCancelledError}process.stdout.write(renderAutocomplete(state,opts)+`\n`);return state.query}async function fromSchema(schema,provided){const result={};const flags=schema.flags??{};for(const[name,def]of Object.entries(flags)){if(!def.required||def.type===\"boolean\")continue;if(provided.has(name))continue;const message=def.description??`Enter a value for --${name}`;if(def.multiple&&def.complete?.type===\"static\"){result[name]=await multiSelect({message,choices:def.complete.values})}else if(def.complete?.type===\"static\"){if(def.complete.values.length<=8){result[name]=await select({message,choices:def.complete.values})}else{result[name]=await autocomplete({message,choices:def.complete.values})}}else if(def.complete?.type===\"dynamic\"){const fetcher=def.complete.fetch;result[name]=await autocomplete({message,choices:(query,signal)=>fetcher({partial:query,flags:result})})}else{result[name]=await text({message})}if(def.type===\"number\"&&typeof result[name]===\"string\"){result[name]=Number(result[name])}}return result}function promptedToArgv(prompted){const argv=[];for(const[key,value]of Object.entries(prompted)){if(Array.isArray(value)){for(const v of value)argv.push(`--${key}`,String(v))}else if(value!==undefined&&value!==null){argv.push(`--${key}`,String(value))}}return argv}function defineConfig(config){return config}function defineConfigSchema(schema){return schema}var executionMode=globalThis[\"__CAPE_EXECUTION_MODE__\"]??\"run\";import{join as join8}from\"path\";import{homedir as homedir3}from\"os\";import{mkdir as mkdir3}from\"fs/promises\";import{join as join6,isAbsolute as isAbsolute2}from\"path\";import{existsSync}from\"fs\";async function findLocalConfigDir(cliName){const filename=`.${cliName}.toml`;let dir=process.cwd();while(true){if(existsSync(join6(dir,filename)))return dir;if(existsSync(join6(dir,\".git\")))break;const parent=join6(dir,\"..\");if(parent===dir)break;dir=parent}return null}async function readFrameworkConfig(cliName){const userConfigDir=join6(xdgConfigHome(),cliName);const localConfigDir=await findLocalConfigDir(cliName);const[userDoc,localDoc]=await Promise.all([readTomlFile(join6(userConfigDir,\"config.toml\")),localConfigDir?readTomlFile(join6(localConfigDir,`.${cliName}.toml`)):Promise.resolve({})]);resolveFrameworkPluginDirs(userDoc,cliName,userConfigDir);if(localConfigDir)resolveFrameworkPluginDirs(localDoc,cliName,localConfigDir);const merged=mergeDocuments(userDoc,localDoc);return merged[cliName]??{}}function resolveFrameworkPluginDirs(doc,cliName,baseDir){const section=doc[cliName];if(!isPlainObject2(section))return;const dirs=section[\"pluginDirs\"];if(!Array.isArray(dirs))return;section[\"pluginDirs\"]=dirs.map((d)=>{if(typeof d!==\"string\")return d;const expanded=expandHome(d);return isAbsolute2(expanded)?expanded:join6(baseDir,expanded)})}async function loadConfig(cliName,commandSection,options){let merged;if(options?.overridePath){merged=await readTomlFile(options.overridePath)}else{const userPath=join6(xdgConfigHome(),cliName,\"config.toml\");const[userDoc,localDoc]=await Promise.all([readTomlFile(userPath),findLocalConfig(cliName)]);merged=mergeDocuments(userDoc,localDoc)}const{[cliName]:_framework,...rawConfig}=merged;const rawCommandConfig=merged[commandSection]??{};return{config:applyDefaults(rawConfig,options?.cliSchema),commandConfig:applyDefaults(rawCommandConfig,options?.commandSchema)}}async function findLocalConfig(cliName){const dir=await findLocalConfigDir(cliName);if(!dir)return{};return readTomlFile(join6(dir,`.${cliName}.toml`))}async function readTomlFile(path){const file=Bun.file(path);if(!await file.exists())return{};try{return parseToml(await file.text())}catch{return{}}}function mergeDocuments(base,override){const result={...base};for(const[key,value]of Object.entries(override)){const baseValue=result[key];if(isPlainObject2(value)&&isPlainObject2(baseValue)){result[key]={...baseValue,...value}}else{result[key]=value}}return result}function isPlainObject2(v){return v!==null&&typeof v===\"object\"&&!Array.isArray(v)}function applyDefaults(values,schema){if(!schema)return values;const result={};for(const[key,field]of Object.entries(schema)){if(field.type===\"object\"){const nested=values[key]??{};result[key]=applyDefaults(nested,field.fields)}else if(field.type===\"array\"){result[key]=values[key]!==undefined?values[key]:field.default??[]}else{result[key]=values[key]!==undefined?values[key]:field.default}}return result}class BasicRuntime{args;env;output;fs;stdin;log;signal;secrets;config={};commandConfig={};prompt;http;_signalManager;_exitHandlers=[];_flushJson;constructor(opts){this.args=opts.args;this.env=isolateEnv(opts.rawEnv,opts.schema);const{globals,cliName,commandName}=opts;if(globals.json){const jsonOut=createJsonOutput(globals.quiet);this.output=jsonOut;this._flushJson=()=>jsonOut.flushJson()}else{this.output=createOutput({noColor:globals.noColor,quiet:globals.quiet,isTTY:!!process.stdout.isTTY})}this.fs=createFs(cliName);this.stdin=createStdin();this.log=createLog({verbose:globals.verbose,debug:globals.debug,noColor:globals.noColor});this.secrets=createSecrets(cliName,commandName);this._signalManager=createSignalManager();this.signal=this._signalManager.signal;this.http=createHttp(this.signal);this.prompt={text:(opts2)=>text({...opts2,signal:this.signal}),confirm:(opts2)=>confirm({...opts2,signal:this.signal}),select:(opts2)=>select({...opts2,signal:this.signal}),multiSelect:(opts2)=>multiSelect({...opts2,signal:this.signal}),autocomplete:(opts2)=>autocomplete({...opts2,signal:this.signal}),NonTtyError,PromptCancelledError}}flushOutput(){this._flushJson?.()}teardown(){this._signalManager.teardown()}async loadConfig(cliName,commandSection,opts){const result=await loadConfig(cliName,commandSection,opts);this.config=result.config;this.commandConfig=result.commandConfig}print(text2){this.output.print(text2)}printError(text2){this.output.printError(text2)}exit(code){this._flushJson?.();process.exit(code)}onExit(fn){this._signalManager.onExit(fn)}}function isolateEnv(rawEnv,schema){const declared=schema?.env;if(!declared)return rawEnv;const result={};for(const key of declared){if(rawEnv[key]!==undefined)result[key]=rawEnv[key]}return result}import{join as join7,relative,isAbsolute as isAbsolute3}from\"path\";import{mkdir as mkdir2}from\"fs/promises\";import{homedir as homedir2}from\"os\";var CAPE_TYPES=`/**\n * Cape \\u2014 CLI Application Plugin Engine\n * Public API type declarations.\n *\n * Auto-generated from source by prebuild.ts. Do not edit.\n * Run \\`bun run cape:prebuild\\` to regenerate.\n */\n\n/**\n * Config schema declared on a \\`CliConfig\\` (top-level keys) or a \\`CommandDef\\`\n * (command-scoped section).  Keys map to their field definitions.\n */\nexport type ConfigSchema = Record<string, ConfigField>;\n/** A scalar (string / number / boolean) config field. */\nexport interface ConfigScalarField {\n    type: \"string\" | \"number\" | \"boolean\";\n    description?: string;\n    default?: string | number | boolean;\n}\n/** An array config field whose items are described by a nested ConfigField. */\nexport interface ConfigArrayField {\n    type: \"array\";\n    /** Schema for each element \\u2014 any ConfigField variant, including object. */\n    items: ConfigField;\n    description?: string;\n    default?: unknown[];\n}\n/** A nested-object config field whose sub-keys are declared in \\`fields\\`. */\nexport interface ConfigObjectField {\n    type: \"object\";\n    description?: string;\n    fields: ConfigSchema;\n}\n/**\n * A single config file key \\u2014 a scalar value, an array, or a nested object.\n * Discriminated on \\`type\\`.\n */\nexport type ConfigField = ConfigScalarField | ConfigArrayField | ConfigObjectField;\n/** Context passed to dynamic completion fetchers. */\nexport interface CompletionCtx {\n    /** The partial word being completed. */\n    partial: string;\n    /** Flag values already typed on the command line (best-effort, may be incomplete). */\n    flags: Record<string, unknown>;\n}\nexport type CompletionSource = {\n    type: \"static\";\n    values: string[];\n} | {\n    type: \"dynamic\";\n    fetch: (ctx: CompletionCtx) => Promise<string[]>;\n    /** How long to cache results (ms). Omit to disable caching. */\n    cacheMs?: number;\n    /** Abort and return [] if fetch takes longer than this (ms). Default: 5000. */\n    timeoutMs?: number;\n    /** Other flag names whose values should be available in ctx.flags before fetching. */\n    dependsOn?: string[];\n};\nexport interface ArgSchema {\n    positionals?: {\n        name: string;\n        variadic?: boolean;\n        complete?: CompletionSource;\n    }[];\n    flags?: {\n        [name: string]: {\n            type: \"boolean\" | \"string\" | \"number\";\n            alias?: string;\n            required?: boolean;\n            multiple?: boolean;\n            default?: unknown;\n            description?: string;\n            complete?: CompletionSource;\n            hideInSubcommandHelp?: boolean;\n        };\n    };\n    /**\n     * Environment variable names to expose on \\`runtime.env\\`.\n     * When declared, only these variables are visible to the command.\n     * When omitted, all environment variables are passed through.\n     */\n    env?: string[];\n}\nexport interface ParsedArgs {\n    flags: Record<string, unknown>;\n    positionals: string[];\n    passthrough: string[];\n    /** Flag names that were explicitly set by the user (excludes defaults). */\n    provided: Set<string>;\n}\nexport type TokenType = \"flag\" | \"value\" | \"separator\";\nexport interface Token {\n    type: TokenType;\n    raw: string;\n}\n\n/**\n * Converts raw argv into a flat token stream.\n *\n * Rules:\n * - \"--\" \\u2192 separator token; all subsequent args become values (passthrough)\n * - \"--flag=value\" \\u2192 two tokens: flag \"--flag\", value \"value\"\n * - \"--flag\" / \"-f\" \\u2192 flag token\n * - \"-abc\" \\u2192 cluster, expanded to flag tokens \"-a\", \"-b\", \"-c\"\n * - anything else \\u2192 value token\n */\nexport declare function tokenize(argv: string[]): Token[];\n\nexport declare class ParseError extends Error {\n    readonly suggestion?: string | undefined;\n    constructor(message: string, suggestion?: string | undefined);\n}\nexport interface ResolveOptions {\n    /**\n     * When true, skips the required-flag and required-positional checks.\n     * Useful for partial parsing (e.g. before interactive prompting fills in\n     * missing required values).\n     */\n    skipRequired?: boolean;\n}\nexport declare function resolve(tokens: Token[], schema: ArgSchema, opts?: ResolveOptions): ParsedArgs;\n\n/** The framework's built-in flags, available on every command. */\nexport declare const globalSchema: ArgSchema;\nexport interface GlobalFlags {\n    help: boolean;\n    version: boolean;\n    json: boolean;\n    noColor: boolean;\n    quiet: boolean;\n    verbose: boolean;\n    debug: boolean;\n    config: string | undefined;\n}\n/**\n * Merges two schemas. \\`overlay\\` wins on flag name collision (command flags\n * shadow global flags). Positionals come from \\`overlay\\` only \\u2014 global flags\n * never declare positionals.\n */\nexport declare function mergeSchemas(base: ArgSchema, overlay: ArgSchema): ArgSchema;\n/**\n * Extracts typed global flag values from a parsed args object.\n * If \\`--debug\\` is set, \\`verbose\\` is also considered active.\n */\nexport declare function extractGlobalFlags(parsed: ParsedArgs): GlobalFlags;\n\nexport interface TableOptions {\n    /** Explicit column order. Defaults to keys of the first row. */\n    columns?: string[];\n}\nexport interface Spinner {\n    /** Update the spinner message while it's running. */\n    update(message: string): void;\n    /** Stop and print a success line. */\n    succeed(message?: string): void;\n    /** Stop and print a failure line. */\n    fail(message?: string): void;\n    /** Stop and erase the spinner without printing anything. */\n    stop(): void;\n}\nexport interface ProgressBar {\n    /** Advance by n (default 1). */\n    tick(n?: number): void;\n    /** Update the total. */\n    setTotal(total: number): void;\n    /** Mark complete and optionally print a final message. */\n    done(message?: string): void;\n}\nexport interface OutputInterface {\n    /** Raw text line \\u2014 same as runtime.print(). */\n    print(text: string): void;\n    /** Raw error line \\u2014 same as runtime.printError(). */\n    printError(text: string): void;\n    /** Green checkmark + message. Silent when --quiet. */\n    success(message: string): void;\n    /** Yellow warning + message. Always shown. */\n    warn(message: string): void;\n    /** Emit \\`value\\` as pretty-printed JSON. Bypasses --quiet. */\n    json(value: unknown): void;\n    /**\n     * Render an array of row objects as a table.\n     * TTY \\u2192 box-drawing table. Pipe \\u2192 tab-separated values.\n     */\n    table(rows: Record<string, unknown>[], opts?: TableOptions): void;\n    /** Render a list of strings. TTY \\u2192 bullets, pipe \\u2192 plain lines. */\n    list(items: string[]): void;\n    /** Start a spinner with an initial message. */\n    spinner(message: string): Spinner;\n    /** Run \\`fn\\`, show a spinner while it executes, then stop. */\n    withSpinner<T>(message: string, fn: (spinner: Spinner) => Promise<T>): Promise<T>;\n    /** Create a progress bar with the given total. */\n    progressBar(total: number): ProgressBar;\n    /** Run \\`fn\\` with a tick callback, show progress while it executes. */\n    withProgressBar<T>(total: number, fn: (tick: (n?: number) => void) => Promise<T>): Promise<T>;\n}\ninterface JsonOutputInternal extends OutputInterface {\n    /** Flush the accumulated output as a single JSON value to stdout. */\n    flushJson(): void;\n}\n/**\n * Creates an output that accumulates calls and emits a single JSON object.\n *\n * Flush shape:\n *   - texts=[], results=[v]         \\u2192 v  (unwrapped single value)\n *   - texts=[], results=[v1,v2]     \\u2192 { results: [v1, v2] }\n *   - texts=[t], results=[]         \\u2192 { output: [t] }\n *   - texts=[t], results=[v]        \\u2192 { output: [t], results: [v] }\n *\n * Texts are suppressed when quiet=true (results still captured).\n * printError / warn always write to stderr (not captured).\n */\nexport declare function createJsonOutput(quiet: boolean): JsonOutputInternal;\ninterface OutputOptions {\n    noColor: boolean;\n    quiet: boolean;\n    /** Whether stdout is a TTY (for table/spinner/progress rendering). */\n    isTTY: boolean;\n}\nexport declare function createOutput(opts: OutputOptions): OutputInterface;\nexport type OutputCall = {\n    type: \"print\";\n    text: string;\n} | {\n    type: \"printError\";\n    text: string;\n} | {\n    type: \"success\";\n    message: string;\n} | {\n    type: \"warn\";\n    message: string;\n} | {\n    type: \"json\";\n    value: unknown;\n} | {\n    type: \"table\";\n    rows: Record<string, unknown>[];\n    opts?: TableOptions;\n} | {\n    type: \"list\";\n    items: string[];\n} | {\n    type: \"spinner\";\n    message: string;\n} | {\n    type: \"progressBar\";\n    total: number;\n};\n/** Mock OutputInterface that records all calls for use in tests. */\nexport declare function createMockOutput(): OutputInterface & {\n    calls: OutputCall[];\n};\nexport {};\n\nexport interface FsInterface {\n    /** Read a file as a UTF-8 string. */\n    read(path: string): Promise<string>;\n    /** Read a file as raw bytes. */\n    readBytes(path: string): Promise<Uint8Array>;\n    /**\n     * Write a file.  Parent directory is created automatically.\n     * @param mode Optional Unix permissions, e.g. 0o600. Defaults to 0o644.\n     */\n    write(path: string, content: string | Uint8Array, mode?: number): Promise<void>;\n    /** Returns true if the path exists (file or directory). */\n    exists(path: string): Promise<boolean>;\n    /** List entries in a directory. Returns basenames only. */\n    list(path: string): Promise<string[]>;\n    /** Path under \\`$XDG_CONFIG_HOME/<cliName>\\` (or \\`~/.config/<cliName>\\`). */\n    configPath(...segments: string[]): string;\n    /** Path under \\`$XDG_DATA_HOME/<cliName>\\` (or \\`~/.local/share/<cliName>\\`). */\n    dataPath(...segments: string[]): string;\n    /** Path under \\`$XDG_CACHE_HOME/<cliName>\\` (or \\`~/.cache/<cliName>\\`). */\n    cachePath(...segments: string[]): string;\n}\nexport declare function createFs(cliName: string): FsInterface;\nexport type MockFsEntry = string | Uint8Array;\n/** Mock FsInterface backed by an in-memory Map. */\nexport declare function createMockFs(cliName?: string): FsInterface & {\n    files: Map<string, MockFsEntry>;\n};\nexport declare function xdgConfigHome(): string;\nexport declare function xdgDataHome(): string;\nexport declare function xdgCacheHome(): string;\n/** Expands a leading \\`~/\\` or bare \\`~\\` to the user's home directory. */\nexport declare function expandHome(p: string): string;\n\nexport interface StdinInterface {\n    /** True when stdin is connected to a terminal. */\n    readonly isTTY: boolean;\n    /**\n     * Read all of stdin and return it as a string.\n     * On a TTY this reads until the user presses Ctrl+D.\n     */\n    read(): Promise<string>;\n    /**\n     * Async iterable of lines from stdin.\n     * Each value is a line without the trailing newline.\n     */\n    lines(): AsyncIterable<string>;\n}\nexport declare function createStdin(): StdinInterface;\n/** Mock StdinInterface that reads from a pre-set string. */\nexport declare function createMockStdin(content?: string, isTTY?: boolean): StdinInterface;\n\nexport interface LogInterface {\n    /**\n     * Log a verbose message.\n     * Only printed when the user passed \\`--verbose\\` or \\`--debug\\`.\n     */\n    verbose(message: string): void;\n    /**\n     * Log a debug message.\n     * Only printed when the user passed \\`--debug\\`.\n     */\n    debug(message: string): void;\n}\ninterface LogOptions {\n    verbose: boolean;\n    debug: boolean;\n    noColor: boolean;\n}\nexport declare function createLog(opts: LogOptions): LogInterface;\nexport type LogCall = {\n    level: \"verbose\" | \"debug\";\n    message: string;\n};\n/** Mock LogInterface that records all log calls for use in tests. */\nexport declare function createMockLog(): LogInterface & {\n    calls: LogCall[];\n};\nexport {};\n\nexport interface SecretsInterface {\n    /** Get a secret value scoped to the current command. */\n    get(key: string): Promise<string | undefined>;\n    /** Set a secret value scoped to the current command. */\n    set(key: string, value: string): Promise<void>;\n    /** Delete a secret value scoped to the current command. */\n    delete(key: string): Promise<void>;\n}\nexport declare function createSecrets(cliName: string, commandName: string): SecretsInterface;\n/** Mock SecretsInterface backed by an in-memory Map. */\nexport declare function createMockSecrets(initial?: Record<string, string>): SecretsInterface & {\n    store: Map<string, string>;\n};\n\nexport type Key = {\n    type: \"char\";\n    char: string;\n} | {\n    type: \"up\";\n} | {\n    type: \"down\";\n} | {\n    type: \"left\";\n} | {\n    type: \"right\";\n} | {\n    type: \"enter\";\n} | {\n    type: \"backspace\";\n} | {\n    type: \"delete\";\n} | {\n    type: \"home\";\n} | {\n    type: \"end\";\n} | {\n    type: \"tab\";\n} | {\n    type: \"escape\";\n} | {\n    type: \"interrupt\";\n};\n/** Thrown when a prompt is invoked but stdin is not a TTY. */\nexport declare class NonTtyError extends Error {\n    constructor(message?: string);\n}\n/** Thrown when the user cancels a prompt (Ctrl+C or Escape). */\nexport declare class PromptCancelledError extends Error {\n    constructor();\n}\nexport interface TextPromptOptions {\n    message: string;\n    /** Pre-filled default value shown in the input. */\n    default?: string;\n    /** Return an error message string to reject the value, or undefined to accept. */\n    validate?: (value: string) => string | undefined;\n    signal?: AbortSignal;\n}\nexport interface SelectPromptOptions {\n    message: string;\n    choices: string[];\n    /** Initially highlighted choice. */\n    default?: string;\n    signal?: AbortSignal;\n}\nexport interface ConfirmPromptOptions {\n    message: string;\n    /** If true, Enter accepts \\`true\\`; if false, Enter accepts \\`false\\`. Default: false. */\n    default?: boolean;\n    signal?: AbortSignal;\n}\nexport interface MultiSelectPromptOptions {\n    message: string;\n    choices: string[];\n    /** Pre-checked choices. */\n    defaults?: string[];\n    signal?: AbortSignal;\n}\nexport interface AutocompletePromptOptions {\n    message: string;\n    /**\n     * Static array of choices filtered locally, or an async function called on\n     * each query change with debouncing. The function receives the current query\n     * and an AbortSignal (aborted when a new query supersedes this one).\n     */\n    choices: string[] | ((query: string, signal: AbortSignal) => Promise<string[]>);\n    default?: string;\n    /** Debounce delay for dynamic fetchers (ms). Default: 150. */\n    debounceMs?: number;\n    signal?: AbortSignal;\n}\n/**\n * All interactive prompt methods, pre-bound to the command's AbortSignal.\n * Access via \\`runtime.prompt\\` \\u2014 no separate imports needed.\n *\n * All methods throw NonTtyError when stdin is not a TTY, and\n * PromptCancelledError when the user presses Ctrl+C or Escape.\n */\nexport interface PromptInterface {\n    /** Free-form text input. */\n    text(options: Omit<TextPromptOptions, \"signal\">): Promise<string>;\n    /** Yes/no confirmation. */\n    confirm(options: Omit<ConfirmPromptOptions, \"signal\">): Promise<boolean>;\n    /** Single choice from a list. */\n    select(options: Omit<SelectPromptOptions, \"signal\">): Promise<string>;\n    /** Multiple choices from a list. */\n    multiSelect(options: Omit<MultiSelectPromptOptions, \"signal\">): Promise<string[]>;\n    /** Text input with live-filtered suggestions (static list or async fetch). */\n    autocomplete(options: Omit<AutocompletePromptOptions, \"signal\">): Promise<string>;\n    /** Thrown when a prompt is invoked outside a TTY. */\n    NonTtyError: typeof NonTtyError;\n    /** Thrown when the user cancels a prompt (Ctrl+C or Escape). */\n    PromptCancelledError: typeof PromptCancelledError;\n}\n\nexport interface Runtime {\n    /** Parsed args for the current command invocation. */\n    args: ParsedArgs;\n    /**\n     * Environment variables available to this command.\n     * When the command schema declares \\`env: [\"FOO\", \"BAR\"]\\`, only those\n     * variables are exposed.  Otherwise all env vars pass through.\n     */\n    env: Record<string, string>;\n    /** Write a line to stdout. */\n    print(text: string): void;\n    /** Write a line to stderr. */\n    printError(text: string): void;\n    /** Exit the process with the given exit code. */\n    exit(code: number): never;\n    /** Rich output interface \\u2014 table, list, json, success, warn, spinner, progress. */\n    output: OutputInterface;\n    /** Filesystem helpers with XDG path utilities. */\n    fs: FsInterface;\n    /** Stdin access \\u2014 isTTY check, read, line iteration. */\n    stdin: StdinInterface;\n    /** Verbose / debug logging wired to --verbose / --debug global flags. */\n    log: LogInterface;\n    /**\n     * AbortSignal that is aborted when SIGINT or SIGTERM is received.\n     * Pass this to fetch(), long-running loops, or async operations so they\n     * can cancel cleanly.\n     */\n    signal: AbortSignal;\n    /**\n     * Register a cleanup function that runs when the process receives SIGINT or\n     * SIGTERM.  Handlers run in reverse registration order (last-in, first-out).\n     */\n    onExit(fn: () => void | Promise<void>): void;\n    /** Secrets interface \\u2014 get/set/delete values from credentials.toml. */\n    secrets: SecretsInterface;\n    /** Top-level config from \\`~/.config/<cli>/config.toml\\`. */\n    config: Record<string, unknown>;\n    /** Command-specific section from config.toml (\\`[command-name]\\`). */\n    commandConfig: Record<string, unknown>;\n    /**\n     * Interactive prompt helpers \\u2014 pre-bound to this command's AbortSignal.\n     * No separate imports needed; use \\`runtime.prompt.text(...)\\` etc.\n     */\n    prompt: PromptInterface;\n}\n\ninterface MockRuntimeOptions {\n    args?: Partial<ParsedArgs>;\n    env?: Record<string, string>;\n    stdinContent?: string;\n    stdinIsTTY?: boolean;\n    secrets?: Record<string, string>;\n    config?: Record<string, unknown>;\n    commandConfig?: Record<string, unknown>;\n    /** Virtual filesystem entries: path \\u2192 content. */\n    files?: Record<string, MockFsEntry>;\n    cliName?: string;\n}\nexport declare class MockRuntime implements Runtime {\n    args: ParsedArgs;\n    env: Record<string, string>;\n    output: OutputInterface;\n    fs: FsInterface;\n    stdin: StdinInterface;\n    log: LogInterface;\n    signal: AbortSignal;\n    secrets: SecretsInterface;\n    config: Record<string, unknown>;\n    commandConfig: Record<string, unknown>;\n    prompt: PromptInterface;\n    readonly printed: string[];\n    readonly errors: string[];\n    exitCode: number | undefined;\n    /** All output method calls, in order. */\n    get outputCalls(): OutputCall[];\n    /** All log calls. */\n    get logCalls(): LogCall[];\n    /** Secret store (readable in tests). */\n    get secretStore(): Map<string, string>;\n    /** Virtual filesystem (readable / writable in tests). */\n    get fsFiles(): Map<string, MockFsEntry>;\n    private _signalManager;\n    constructor(options?: MockRuntimeOptions);\n    print(text: string): void;\n    printError(text: string): void;\n    exit(code: number): never;\n    onExit(fn: () => void | Promise<void>): void;\n    /** Simulate receiving SIGINT / SIGTERM. Runs all registered onExit handlers. */\n    abort(): Promise<void>;\n}\nexport declare class MockExitError extends Error {\n    readonly code: number;\n    constructor(code: number);\n}\nexport {};\n\nexport interface CliInfo {\n    name: string;\n    displayName?: string;\n    version?: string;\n    description: string;\n}\nexport interface CommandSummary {\n    name: string;\n    aliases?: string[];\n    description: string;\n}\nexport interface CommandDetail {\n    name: string;\n    description: string;\n    schema: ArgSchema;\n}\nexport type HelpContext = {\n    level: \"root\";\n    commands: CommandSummary[];\n} | {\n    level: \"command\";\n    command: CommandDetail;\n    subcommands: CommandSummary[];\n} | {\n    level: \"subcommand\";\n    command: CommandDetail;\n    subcommand: CommandDetail;\n};\nexport interface RenderOptions {\n    noColor?: boolean;\n}\n\nexport declare function renderHelp(cli: CliInfo, ctx: HelpContext, opts?: RenderOptions): string;\n\n/** The shape of a *.plugin.toml file \\u2014 cheap to read, no JS import needed. */\nexport interface MinimalManifest {\n    name: string;\n    description: string;\n    /** Path to the TS command file, relative to the *.plugin.toml location. */\n    command: string;\n    enabled: boolean;\n    frameworkVersion: string;\n}\n/** A plugin found on disk but not yet imported. */\nexport interface DiscoveredPlugin {\n    manifest: MinimalManifest;\n    /** Absolute path to the directory containing the *.plugin.toml file. */\n    pluginDir: string;\n    /** Absolute path to the *.plugin.toml file itself. */\n    manifestPath: string;\n}\n\nexport interface DiscoverOptions {\n    /** When true, disabled plugins are included in results. Default: false. */\n    includeDisabled?: boolean;\n}\n/**\n * Recursively scans \\`dirs\\` for *.plugin.toml files and returns all valid,\n * enabled plugins. Directories that don't exist are silently skipped.\n * Malformed manifests are warned about and skipped.\n */\nexport declare function discoverPlugins(dirs: string[], opts?: DiscoverOptions): Promise<DiscoveredPlugin[]>;\n\n/**\n * The shape of a single flag definition \\u2014 mirrors ArgSchema flags entry\n * but kept explicit here so the inference types can reference it directly.\n */\nexport type FlagDef = {\n    type: \"boolean\" | \"string\" | \"number\";\n    alias?: string;\n    required?: boolean;\n    multiple?: boolean;\n    default?: unknown;\n    description?: string;\n    complete?: CompletionSource;\n    hideInSubcommandHelp?: boolean;\n};\n/** Maps \"boolean\" | \"string\" | \"number\" to their TypeScript types. */\ntype FlagBaseType<T extends \"boolean\" | \"string\" | \"number\"> = T extends \"boolean\" ? boolean : T extends \"string\" ? string : T extends \"number\" ? number : never;\n/**\n * Infers the TypeScript type of a single parsed flag value.\n *\n * - multiple: true         \\u2192 array\n * - boolean               \\u2192 always boolean (defaults to false, never undefined)\n * - required: true         \\u2192 never undefined\n * - default provided       \\u2192 never undefined\n * - otherwise              \\u2192 value | undefined\n */\ntype InferFlagValue<F extends FlagDef> = F[\"multiple\"] extends true ? FlagBaseType<F[\"type\"]>[] : F[\"type\"] extends \"boolean\" ? boolean : F[\"required\"] extends true ? FlagBaseType<F[\"type\"]> : F extends {\n    default: NonNullable<unknown>;\n} ? FlagBaseType<F[\"type\"]> : FlagBaseType<F[\"type\"]> | undefined;\n/** Infers the typed flags record from a flags schema object. */\ntype InferFlags<F extends Record<string, FlagDef>> = {\n    [K in keyof F]: InferFlagValue<F[K]>;\n};\n/**\n * Infers a fully typed ParsedArgs from an ArgSchema.\n * Falls back to the untyped ParsedArgs if the flags shape can't be resolved.\n */\nexport type InferParsedArgs<S extends ArgSchema> = S[\"flags\"] extends Record<string, FlagDef> ? {\n    flags: InferFlags<S[\"flags\"]>;\n    positionals: string[];\n    passthrough: string[];\n} : ParsedArgs;\ntype ScalarType = \"string\" | \"number\" | \"boolean\";\ntype ConfigBaseType<T extends ScalarType> = T extends \"string\" ? string : T extends \"number\" ? number : T extends \"boolean\" ? boolean : never;\n/**\n * Infers the element type for an array field \\u2014 same logic as InferConfigValue\n * but without the optional/required wrapping (the array itself carries that).\n * Supports scalars, objects, and nested arrays.\n */\ntype InferArrayItemType<F extends ConfigField> = F extends {\n    type: \"object\";\n    fields: infer FS extends ConfigSchema;\n} ? InferConfig<FS> : F extends {\n    type: \"array\";\n    items: infer I extends ConfigField;\n} ? InferArrayItemType<I>[] : F extends ConfigScalarField ? ConfigBaseType<F[\"type\"]> : never;\n/**\n * Infers the TypeScript type of a single config value.\n *\n * - type: \"object\"   \\u2192 recursively infer nested ConfigSchema\n * - type: \"array\"    \\u2192 typed array of the inferred item type\n * - default provided \\u2192 value is always present (never undefined)\n * - no default       \\u2192 value may be undefined (key absent from all config files)\n */\ntype InferConfigValue<F extends ConfigField> = F extends {\n    type: \"object\";\n    fields: infer FS extends ConfigSchema;\n} ? InferConfig<FS> : F extends {\n    type: \"array\";\n    items: infer I extends ConfigField;\n} ? InferArrayItemType<I>[] : F extends ConfigScalarField & {\n    default: NonNullable<unknown>;\n} ? ConfigBaseType<F[\"type\"]> : F extends ConfigScalarField ? ConfigBaseType<F[\"type\"]> | undefined : never;\n/**\n * Infers a typed config record from a ConfigSchema.\n * When the schema is empty (no config declared), falls back to\n * Record<string, unknown> so untyped access still compiles.\n */\nexport type InferConfig<CC extends ConfigSchema> = [\n    keyof CC\n] extends [never] ? Record<string, unknown> : {\n    [K in keyof CC]: InferConfigValue<CC[K]>;\n};\nexport {};\n\nexport interface SubcommandDef {\n    name: string;\n    aliases?: string[];\n    description: string;\n    schema?: ArgSchema;\n    /** Config schema for this subcommand's section in config.toml. */\n    config?: ConfigSchema;\n    run(args: ParsedArgs, runtime: Runtime): Promise<void>;\n}\nexport interface CommandDef {\n    name: string;\n    aliases?: string[];\n    description: string;\n    schema?: ArgSchema;\n    /** Config schema for this command's section in config.toml. */\n    config?: ConfigSchema;\n    subcommands?: SubcommandDef[];\n    run?(args: ParsedArgs, runtime: Runtime): Promise<void>;\n}\n/**\n * A Runtime with both \\`commandConfig\\` and \\`config\\` narrowed to their inferred types.\n * \\`CC\\` is the command-level config schema; \\`GC\\` is the CLI-level (global) config schema.\n */\ntype RuntimeWith<CC extends ConfigSchema, GC extends ConfigSchema> = Omit<Runtime, \"commandConfig\" | \"config\"> & {\n    commandConfig: InferConfig<CC>;\n    config: InferConfig<GC>;\n};\n/**\n * Defines a subcommand with schema-inferred arg types.\n * TypeScript captures the exact schema shape and types \\`args\\` in \\`run\\` accordingly \\u2014\n * no casts needed inside the implementation.\n */\nexport declare function defineSubcommand<S extends ArgSchema, CC extends ConfigSchema = Record<never, ConfigField>>(def: {\n    name: string;\n    aliases?: string[];\n    description: string;\n    schema?: S;\n    config?: CC;\n    run(args: InferParsedArgs<S>, runtime: RuntimeWith<CC, Record<never, ConfigField>>): Promise<void>;\n}): SubcommandDef;\n/**\n * Defines a command with schema-inferred arg types.\n * TypeScript captures the exact schema shape and types \\`args\\` in \\`run\\` accordingly \\u2014\n * no casts needed inside the implementation.\n */\nexport declare function defineCommand<S extends ArgSchema, CC extends ConfigSchema = Record<never, ConfigField>>(def: {\n    name: string;\n    aliases?: string[];\n    description: string;\n    schema?: S;\n    config?: CC;\n    subcommands?: SubcommandDef[];\n    run?(args: InferParsedArgs<S>, runtime: RuntimeWith<CC, Record<never, ConfigField>>): Promise<void>;\n}): CommandDef;\n/**\n * Creates typed \\`defineCommand\\` / \\`defineSubcommand\\` helpers pre-bound to the\n * CLI-level config schema \\`GC\\`, so \\`runtime.config\\` is fully typed without\n * repeating the schema on every command.\n *\n * @example\n * \\`\\`\\`ts\n * // shared/_typed.ts\n * import type { myCliConfig } from \"../cli.config.ts\";\n * export const { defineCommand, defineSubcommand } =\n *   typedWith<typeof myCliConfig.config>();\n *\n * // commands/deploy.ts\n * import { defineCommand } from \"../shared/_typed.ts\";\n * export const deployCommand = defineCommand({\n *   name: \"deploy\",\n *   run(_args, runtime) {\n *     const url = runtime.config.apiUrl; // typed!\n *   },\n * });\n * \\`\\`\\`\n */\nexport declare function typedWith<GC extends ConfigSchema>(): {\n    defineCommand<S extends ArgSchema, CC extends ConfigSchema = Record<never, ConfigField>>(def: {\n        name: string;\n        aliases?: string[];\n        description: string;\n        schema?: S;\n        config?: CC;\n        subcommands?: SubcommandDef[];\n        run?(args: InferParsedArgs<S>, runtime: RuntimeWith<CC, GC>): Promise<void>;\n    }): CommandDef;\n    defineSubcommand<S extends ArgSchema, CC extends ConfigSchema = Record<never, ConfigField>>(def: {\n        name: string;\n        aliases?: string[];\n        description: string;\n        schema?: S;\n        config?: CC;\n        run(args: InferParsedArgs<S>, runtime: RuntimeWith<CC, GC>): Promise<void>;\n    }): SubcommandDef;\n};\nexport interface SetupSecret {\n    /** Key written to credentials.toml. */\n    key: string;\n    /** Prompt message shown to the user. */\n    message: string;\n    /** Optional hint printed below the prompt. */\n    description?: string;\n    /** Default value pre-filled in the prompt. */\n    default?: string;\n}\n/**\n * Where and how to publish binaries for \\`cape build\\`'s install.sh generation.\n *\n * @example GitHub Releases\n * install: { type: \"github\", repo: \"myorg/myctl\" }\n *\n * @example Self-hosted\n * install: { type: \"custom\", url: \"https://cli.mycompany.com/releases/v{VERSION}\" }\n * // {VERSION} is replaced with the version string at build time.\n * // Binaries are expected at <url>/<name>-<os>-<arch>\n */\nexport type InstallConfig = {\n    type: \"github\";\n    repo: string;\n} | {\n    type: \"custom\";\n    url: string;\n};\nexport interface CliConfig extends CliInfo {\n    /**\n     * Schema for top-level keys in config.toml (and \\`.{name}.toml\\`).\n     * Declares types, descriptions, and defaults for \\`runtime.config\\`.\n     */\n    config?: ConfigSchema;\n    /**\n     * Additional directories to scan for *.plugin.toml files.\n     * The framework always scans ./commands/ and ~/.config/<name>/plugins/\n     * first \\u2014 these dirs are appended after.\n     */\n    pluginDirs?: string[];\n    /**\n     * Binary distribution configuration for install.sh generation.\n     * Replaces the older \\`repository\\` shorthand.\n     */\n    install?: InstallConfig;\n    /**\n     * @deprecated Use \\`install: { type: \"github\", repo: \"owner/repo\" }\\` instead.\n     * Kept for backwards compatibility.\n     */\n    repository?: string;\n    /**\n     * First-run setup prompts for the built-in \\`init\\` command.\n     * Each entry shows a text prompt and writes the answer to credentials.toml.\n     */\n    setup?: {\n        secrets?: SetupSecret[];\n    };\n}\nexport declare function createCli(config: CliConfig, commands?: CommandDef[]): {\n    run(argv?: string[]): Promise<void>;\n};\nexport {};\n\nexport type ExecutionMode = \"run\" | \"complete\";\n/**\n * The mode the framework set before importing this plugin module.\n * Plugin authors can read this at module level to skip expensive imports\n * that are only needed during execution (not completion).\n *\n * @example\n * import { executionMode } from \"cape\";\n * const client = executionMode === \"run\" ? await import(\"./heavy-sdk.ts\") : null;\n */\nexport declare const executionMode: ExecutionMode;\n\n/** The framework's own version \\u2014 used for plugin compatibility checks. */\nexport declare const FRAMEWORK_VERSION = \"1.0.0\";\n/**\n * Dynamically imports a plugin module and returns its CommandDef.\n * Sets the global execution mode before import so plugins can read it\n * at module level to skip expensive dependencies during completion.\n *\n * Throws if the plugin is incompatible (major version mismatch) or if\n * the module has no default export.\n */\nexport declare function loadPlugin(plugin: DiscoveredPlugin, mode: ExecutionMode): Promise<CommandDef>;\n\n/**\n * Given the tokens already on the command line (before the cursor) and the\n * partial word being completed, returns completion candidates.\n *\n * @param commands  All available commands (static + loaded plugins)\n * @param argv      Tokens already typed, NOT including the partial word\n * @param partial   The word currently being completed (may be \"\")\n * @param cliName   CLI name used to scope the on-disk cache directory\n */\nexport declare function resolveCompletions(commands: CommandDef[], argv: string[], partial: string, cliName?: string): Promise<string[]>;\n\nexport interface TextState {\n    value: string;\n    cursor: number;\n    /**\n     * When true the current value is the default being shown as a placeholder\n     * (rendered dimmed). The first printable keypress replaces it entirely;\n     * cursor movement / backspace switches to normal editing instead.\n     */\n    isPlaceholder: boolean;\n    error: string | undefined;\n    done: boolean;\n    cancelled: boolean;\n}\nexport declare function textReducer(state: TextState, key: Key, validate?: (v: string) => string | undefined): TextState;\nexport declare function text(opts: TextPromptOptions): Promise<string>;\n\nexport interface SelectState {\n    choices: string[];\n    index: number;\n    done: boolean;\n    cancelled: boolean;\n}\nexport declare function selectReducer(state: SelectState, key: Key): SelectState;\nexport declare function renderSelect(state: SelectState, opts: SelectPromptOptions): string;\nexport declare function select(opts: SelectPromptOptions): Promise<string>;\n\nexport interface ConfirmState {\n    answer: boolean | undefined;\n    done: boolean;\n    cancelled: boolean;\n}\nexport declare function confirmReducer(state: ConfirmState, key: Key, defaultValue: boolean): ConfirmState;\nexport declare function confirm(opts: ConfirmPromptOptions): Promise<boolean>;\n\nexport interface MultiSelectState {\n    choices: string[];\n    index: number;\n    checked: Set<number>;\n    done: boolean;\n    cancelled: boolean;\n}\nexport declare function multiSelectReducer(state: MultiSelectState, key: Key): MultiSelectState;\nexport declare function renderMultiSelect(state: MultiSelectState, opts: MultiSelectPromptOptions): string;\nexport declare function multiSelect(opts: MultiSelectPromptOptions): Promise<string[]>;\n\nexport interface AutocompleteState {\n    query: string;\n    queryCursor: number;\n    items: string[];\n    index: number;\n    loading: boolean;\n    done: boolean;\n    cancelled: boolean;\n    /** The default value \\u2014 used as fallback on Enter when items is empty and query is \"\". */\n    defaultValue?: string;\n}\ntype AutocompleteAction = {\n    type: \"key\";\n    key: Key;\n} | {\n    type: \"items\";\n    items: string[];\n};\nexport declare function autocompleteReducer(state: AutocompleteState, action: AutocompleteAction): AutocompleteState;\nexport declare function renderAutocomplete(state: AutocompleteState, opts: AutocompletePromptOptions, \n/** Current spinner frame index \\u2014 incremented externally while loading. */\nspinnerFrame?: number): string;\n/** The autocomplete runner is custom (not using runPromptLoop) because it\n * interleaves key events with async fetch results. */\nexport declare function autocomplete(opts: AutocompletePromptOptions): Promise<string>;\nexport {};\n\n/**\n * Walks an ArgSchema and interactively prompts for any required flags that\n * are not present in \\`provided\\`.\n *\n * Mapping rules:\n *   - boolean flags: skipped (required booleans are not meaningful to prompt for)\n *   - static completion source with \\u22648 choices: select\n *   - static completion source with >8 choices: autocomplete\n *   - dynamic completion source: autocomplete with live fetch\n *   - no completion source: text\n *   - multiple flags with static source: multi-select\n *\n * Returns a plain object whose keys are flag names and values are the prompted\n * values (strings or string[] for multiple flags). Callers are responsible for\n * type coercion (e.g. string \\u2192 number) before merging with parsed args.\n */\nexport declare function fromSchema(schema: ArgSchema, provided: Set<string>): Promise<Record<string, unknown>>;\n/**\n * Converts prompted values to argv tokens that can be appended to an existing\n * argv array and re-parsed through the normal resolve() path.\n *\n * E.g. \\`{ name: \"Alice\", count: 3 }\\` \\u2192 \\`[\"--name\", \"Alice\", \"--count\", \"3\"]\\`\n * Arrays (multiple flags) produce one \\`--flag value\\` pair per element.\n */\nexport declare function promptedToArgv(prompted: Record<string, unknown>): string[];\n\n/**\n * The full configuration for a Cape-based CLI.\n *\n * Used in \\`cli.config.ts\\` in the project root. Pass it directly to\n * \\`createCli()\\` \\u2014 the extra build-only fields (\\`entry\\`, \\`outfile\\`) are\n * silently ignored at runtime.\n */\nexport interface CliConfigDef extends CliConfig {\n    /**\n     * Semantic version string. Required \\u2014 enables \\`--version\\` support.\n     * E.g. \"1.2.3\"\n     */\n    version: string;\n    /**\n     * Entry point file for \\`bun build --compile\\`.\n     * Default: \"./main.ts\" (resolved relative to cli.config.ts).\n     */\n    entry?: string;\n    /**\n     * Output binary filename. Default: \\`name\\`.\n     */\n    outfile?: string;\n}\n/**\n * Defines a CLI configuration with full TypeScript type checking.\n * Place this in \\`cli.config.ts\\` at your project root.\n *\n * @example\n * \\`\\`\\`ts\n * // cli.config.ts\n * import { defineConfig } from \"cape\";\n *\n * export default defineConfig({\n *   name: \"myctl\",\n *   displayName: \"My Control\",\n *   version: \"1.0.0\",\n *   description: \"Manage your infrastructure\",\n * });\n * \\`\\`\\`\n */\nexport declare function defineConfig<T extends CliConfigDef>(config: T): T;\n/**\n * Defines a config schema with preserved literal types \\u2014 no \\`as const\\` needed.\n * Use this to declare the \\`globalConfig\\` in \\`cli.config.ts\\`.\n *\n * @example\n * \\`\\`\\`ts\n * const globalConfig = defineConfigSchema({\n *   apiUrl: { type: \"string\", description: \"Base API URL\" },\n *   retries: { type: \"number\", default: 3 },\n *   tags:    { type: \"array\", items: { type: \"string\" } },\n * });\n * export const { defineCommand } = typedWith<typeof globalConfig>();\n * \\`\\`\\`\n */\nexport declare function defineConfigSchema<S extends ConfigSchema>(schema: S): S;`;function createPluginCommand(cliName,codePluginDirs,version,configSchema){return{name:\"plugin\",description:\"Manage plugins\",subcommands:[listSubcommand(cliName,codePluginDirs),createSubcommand(cliName,version,configSchema),initSubcommand(cliName,version,configSchema)]}}function listSubcommand(cliName,codePluginDirs){return{name:\"list\",description:\"List all discovered plugins\",async run(_args,runtime){const frameworkCfg=await readFrameworkConfig(cliName);const tomlDirs=frameworkCfg[\"pluginDirs\"]??[];const dirs=pluginDirs(cliName,codePluginDirs,tomlDirs);const plugins=await discoverPlugins(dirs,{includeDisabled:true});if(plugins.length===0){runtime.print(\"No plugins found.\");runtime.print(\"\");runtime.print(\"Searched:\");for(const dir of dirs)runtime.print(`  ${dir}`);return}runtime.output.table(plugins.map((p)=>({Name:p.manifest.name,Description:p.manifest.description,Location:p.pluginDir,Status:p.manifest.enabled?\"enabled\":\"disabled\"})))}}}function createSubcommand(cliName,version,configSchema){return{name:\"create\",description:\"Scaffold a new plugin\",schema:{flags:{name:{type:\"string\",alias:\"n\",description:\"Plugin name\"},description:{type:\"string\",alias:\"d\",description:\"Plugin description\"}}},async run(args,runtime){const pluginName=await resolveArg(args.flags[\"name\"],\"Plugin name\",(v)=>{if(!v.trim())return\"Name cannot be empty\";if(!/^[a-z][a-z0-9-]*$/.test(v.trim()))return\"Use lowercase letters, numbers, and hyphens\";return},runtime);const pluginDescription=await resolveArg(args.flags[\"description\"],\"Description\",(v)=>v.trim()?undefined:\"Cannot be empty\",runtime);const tomlDir=await findLocalConfigDir(cliName);const options=await buildLocationOptions(cliName,tomlDir);const chosen=options.length===1?options[0]:await pickLocation(options,runtime);const pluginDir=join7(chosen.resolvedDir,pluginName);await mkdir2(pluginDir,{recursive:true});let importPath=null;if(chosen.tomlDir){const typesDir=join7(chosen.tomlDir,`.${cliName}`);if(!await Bun.file(join7(typesDir,\"index.ts\")).exists()){await generatePluginTypes(cliName,version,configSchema,chosen.tomlDir)}importPath=`${relative(pluginDir,typesDir)}/index.ts`}await Promise.all([Bun.write(join7(pluginDir,`${pluginName}.plugin.toml`),pluginTomlTemplate(pluginName,pluginDescription)),Bun.write(join7(pluginDir,`${pluginName}.ts`),pluginTsTemplate(pluginName,pluginDescription,importPath))]);runtime.output.success(`Created plugin \"${pluginName}\"`);const displayDir=tomlDir&&pluginDir.startsWith(tomlDir)?`./${relative(tomlDir,pluginDir)}/`:pluginDir;runtime.print(`  Location: ${displayDir}`);if(chosen.tomlDir){runtime.print(`  Types:    .${cliName}/  (commit this folder \\u2014 run '${cliName} plugin init' to update)`)}runtime.print(\"\");runtime.print(\"No registration needed \\u2014 auto-discovered the next time you run the CLI.\")}}}async function buildLocationOptions(cliName,tomlDir){const options=[];if(tomlDir){options.push({label:\"./commands/  (project-local)\",resolvedDir:join7(tomlDir,\"commands\"),tomlDir});const localDoc=Bun.TOML.parse(await Bun.file(join7(tomlDir,`.${cliName}.toml`)).text().catch(()=>\"\"));const frameworkSection=localDoc[cliName]??{};const configuredDirs=frameworkSection[\"pluginDirs\"]??[];for(const dir of configuredDirs){const expanded=expandHome(dir);const resolved=isAbsolute3(expanded)?expanded:join7(tomlDir,expanded);const isInsideProject=!relative(tomlDir,resolved).startsWith(\"..\");options.push({label:`${dir}  (from [${cliName}] config)`,resolvedDir:resolved,tomlDir:isInsideProject?tomlDir:null})}}options.push({label:`~/.config/${cliName}/plugins/  (user-level, untyped)`,resolvedDir:join7(xdgConfigHome(),cliName,\"plugins\"),tomlDir:null});return options}async function pickLocation(options,runtime){try{const label=await select({message:\"Where should the plugin be created?\",choices:options.map((o)=>o.label),signal:runtime.signal});return options.find((o)=>o.label===label)}catch(err){if(err instanceof NonTtyError){runtime.printError(\"Error: location required in non-TTY mode. Use --location.\");runtime.exit(1)}throw err}}function initSubcommand(cliName,version,configSchema){return{name:\"init\",description:`Generate .${cliName}/ type helpers for plugins in this repository`,async run(_args,runtime){const tomlDir=await findLocalConfigDir(cliName);if(!tomlDir){runtime.printError(`Error: no .${cliName}.toml found. Run this from a repository that uses ${cliName}.`);runtime.exit(1);return}await generatePluginTypes(cliName,version,configSchema,tomlDir);runtime.output.success(`Generated .${cliName}/`);runtime.print(`  .${cliName}/cape.d.ts  \\u2014 Cape type declarations`);runtime.print(`  .${cliName}/index.ts   \\u2014 Typed defineCommand / defineSubcommand`);runtime.print(\"\");runtime.print(`Commit .${cliName}/ so plugin authors in this repo get fully typed access.`);runtime.print(`Re-run after upgrading ${cliName}: ${cliName} plugin init`)}}}async function generatePluginTypes(cliName,version,schema,tomlDir){const outDir=join7(tomlDir,`.${cliName}`);await mkdir2(outDir,{recursive:true});await Promise.all([Bun.write(join7(outDir,\"cape.d.ts\"),CAPE_TYPES||\"// Run `bun run cape:prebuild` then rebuild your CLI to populate.\\n\"),Bun.write(join7(outDir,\"index.ts\"),pluginIndexTemplate(cliName,version,schema))])}function pluginIndexTemplate(cliName,version,schema){const interfaceBody=schemaToTypeInterface(schema,1);const emptyNote=Object.keys(schema).length===0?`  // No global config defined \\u2014 add fields to ${cliName}'s CliConfig.config schema.\n`:\"\";return[`// Auto-generated by ${cliName} v${version} \\u2014 do not edit.`,`// Regenerate with: ${cliName} plugin init`,``,`import type {`,`  CommandDef,`,`  SubcommandDef,`,`  ArgSchema,`,`  InferParsedArgs,`,`  ConfigField,`,`  ConfigSchema,`,`  Runtime,`,`  InferConfig,`,`} from \"./cape\";`,``,`// ---------------------------------------------------------------------------`,`// Global config \\u2014 shape of runtime.config in plugins for ${cliName}`,`// Regenerate when ${cliName}'s config schema changes: ${cliName} plugin init`,`// ---------------------------------------------------------------------------`,``,`export interface GlobalConfig {`,emptyNote+interfaceBody,`}`,``,`// ---------------------------------------------------------------------------`,`// Plugin runtime`,`// ---------------------------------------------------------------------------`,``,`type PluginRuntime<CC extends ConfigSchema = Record<never, ConfigField>> =`,`  Omit<Runtime, \"commandConfig\" | \"config\"> & {`,`    commandConfig: InferConfig<CC>;`,`    config: GlobalConfig;`,`  };`,``,`// ---------------------------------------------------------------------------`,`// Typed helpers`,`// ---------------------------------------------------------------------------`,``,`export function defineCommand<`,`  S extends ArgSchema,`,`  CC extends ConfigSchema = Record<never, ConfigField>,`,`>(def: {`,`  name: string;`,`  aliases?: string[];`,`  description: string;`,`  schema?: S;`,`  config?: CC;`,`  subcommands?: SubcommandDef[];`,`  run?(args: InferParsedArgs<S>, runtime: PluginRuntime<CC>): Promise<void>;`,`}): CommandDef {`,`  return def as CommandDef;`,`}`,``,`export function defineSubcommand<`,`  S extends ArgSchema,`,`  CC extends ConfigSchema = Record<never, ConfigField>,`,`>(def: {`,`  name: string;`,`  aliases?: string[];`,`  description: string;`,`  schema?: S;`,`  config?: CC;`,`  run(args: InferParsedArgs<S>, runtime: PluginRuntime<CC>): Promise<void>;`,`}): SubcommandDef {`,`  return def as SubcommandDef;`,`}`,``].join(`\n`)}function schemaToTypeInterface(schema,depth){const pad=\"  \".repeat(depth);return Object.entries(schema).map(([key,field])=>{const doc=field.description?`${pad}/** ${field.description} */\n`:\"\";if(field.type===\"object\"){const inner=schemaToTypeInterface(field.fields,depth+1);return`${doc}${pad}${key}: {\n${inner}\n${pad}};`}if(field.type===\"array\"){return`${doc}${pad}${key}: ${arrayItemType(field.items)}[];`}const base=field.type===\"number\"?\"number\":field.type===\"boolean\"?\"boolean\":\"string\";const opt=field.default===undefined?\" | undefined\":\"\";return`${doc}${pad}${key}: ${base}${opt};`}).join(`\n`)}function arrayItemType(item){if(item.type===\"object\"){return`{ ${Object.entries(item.fields).map(([k,f])=>`${k}: ${arrayItemType(f)}`).join(\"; \")} }`}if(item.type===\"array\")return`${arrayItemType(item.items)}[]`;return item.type===\"number\"?\"number\":item.type===\"boolean\"?\"boolean\":\"string\"}function pluginTomlTemplate(name,description){return[`name = \"${name}\"`,`description = ${JSON.stringify(description)}`,`command = \"./${name}.ts\"`,`enabled = true`,`frameworkVersion = \"${FRAMEWORK_VERSION}\"`,\"\"].join(`\n`)}function pluginTsTemplate(name,description,importPath){const importLine=importPath?`import { defineCommand } from \"${importPath}\";`:`// defineCommand is a no-op identity helper \\u2014 no package install needed.\n`+`// eslint-disable-next-line @typescript-eslint/no-explicit-any\n`+`const defineCommand = (def: any) => def;`;const untypedNote=importPath?\"\":`\n  // Note: runtime.config is untyped (Record<string, unknown>)\n`;return[importLine,\"\",`export default defineCommand({`,`  name: \"${name}\",`,`  description: ${JSON.stringify(description)},`,`  schema: {`,`    flags: {`,`      // example: { type: \"string\", description: \"An example flag\" },`,`    },`,`  },`,`  async run(args, runtime) {`,untypedNote,`    runtime.print(\"Running ${name}...\");`,`  },`,`});`,\"\"].join(`\n`)}function pluginDirs(cliName,codePluginDirs,tomlDirs){return[join7(process.cwd(),\"commands\"),join7(homedir2(),\".config\",cliName,\"plugins\"),...codePluginDirs,...tomlDirs]}async function resolveArg(value,message,validate,runtime){if(value!==undefined){const err=validate(value);if(err){runtime.printError(`${message}: ${err}`);runtime.exit(1)}return value.trim()}try{return(await text({message,validate})).trim()}catch(err){if(err instanceof NonTtyError){runtime.printError(`Error: ${message} is required. Use a flag.`);runtime.exit(1)}throw err}}function createBuiltinCommands(cliName,codePluginDirs,version,configSchema){return[createPluginCommand(cliName,codePluginDirs,version,configSchema)]}function generateCompletionScript(cliName,shell){switch(shell){case\"bash\":return bashScript(cliName);case\"zsh\":return zshScript(cliName);case\"fish\":return fishScript(cliName)}}function completionInstallPath(cliName,shell){const home=process.env[\"HOME\"]??\"~\";switch(shell){case\"bash\":return`${home}/.local/share/bash-completion/completions/${cliName}`;case\"zsh\":return`${home}/.zsh/completions/_${cliName}`;case\"fish\":return`${home}/.config/fish/completions/${cliName}.fish`}}function postInstallMessage(cliName,shell,installedPath){switch(shell){case\"bash\":return[`Bash completions installed to: ${installedPath}`,\"\",\"To activate, add this to your ~/.bashrc (if not already present):\",`  [[ -r \"${installedPath}\" ]] && source \"${installedPath}\"`,\"\",\"Then reload your shell:  source ~/.bashrc\"].join(`\n`);case\"zsh\":return[`Zsh completions installed to: ${installedPath}`,\"\",\"To activate, add this to your ~/.zshrc (if not already present):\",`  fpath=(~/.zsh/completions $fpath)`,\"  autoload -Uz compinit && compinit\",\"\",\"Then reload your shell:  source ~/.zshrc\"].join(`\n`);case\"fish\":return[`Fish completions installed to: ${installedPath}`,\"\",`Fish automatically loads completions from this directory.`,\"Open a new terminal or run:  exec fish\"].join(`\n`)}}function detectShell(){const shellPath=process.env[\"SHELL\"]??\"\";if(shellPath.endsWith(\"zsh\"))return\"zsh\";if(shellPath.endsWith(\"bash\"))return\"bash\";if(shellPath.endsWith(\"fish\"))return\"fish\";return}function bashScript(name){const S=\"$\";const fn=safeName(name);return`# ${name} bash completion\n`+`# Generated by ${name} completions generate --shell bash\n`+`# Install: ${name} completions install --shell bash\n`+`\n`+`_${fn}_completions() {\n`+`  local words=(\"${S}{COMP_WORDS[@]:1}\")  # drop the binary name\n`+`  local cword=$((COMP_CWORD - 1))\n`+`  [[ $cword -lt 0 ]] && return\n`+`\n`+`  mapfile -t COMPREPLY < <(${name} __complete \"$cword\" \"${S}{words[@]}\" 2>/dev/null)\n`+`}\n`+`\n`+`complete -o nospace -F _${fn}_completions ${name}\n`}function zshScript(name){const S=\"$\";const fn=safeName(name);return`#compdef ${name}\n`+`# ${name} zsh completion\n`+`# Generated by ${name} completions generate --shell zsh\n`+`# Install: ${name} completions install --shell zsh\n`+`\n`+`_${fn}_completions() {\n`+`  local -a _words\n`+`  # words[] is 1-indexed in zsh; words[1] is the binary name\n`+`  _words=(\"${S}{(@)words[2,-1]}\")\n`+`  local _cword=$((CURRENT - 2))\n`+`  [[ $_cword -lt 0 ]] && return\n`+`\n`+`  local -a _completions\n`+`  _completions=(${S}{(f)\"${S}(${name} __complete \"${S}_cword\" \"${S}{_words[@]}\" 2>/dev/null)\"})\n`+`  compadd -a _completions\n`+`}\n`+`\n`+`_${fn}_completions\n`}function fishScript(name){const S=\"$\";const fn=safeName(name);return`# ${name} fish completion\n`+`# Generated by ${name} completions generate --shell fish\n`+`# Install: ${name} completions install --shell fish\n`+`\n`+`function __${fn}_complete\n`+`  # commandline -opc: tokens before cursor, including the binary name\n`+`  # commandline -ct:  current (partial) token\n`+`  set -l opc (commandline -opc)\n`+`  set -l partial (commandline -ct)\n`+`  set -l prev ${S}opc[2..-1]    # drop binary name\n`+`  set -l cword (count ${S}prev) # partial is at index = count of previous tokens\n`+`  ${name} __complete ${S}cword ${S}prev ${S}partial 2>/dev/null\n`+`end\n`+`\n`+`complete -c ${name} -f -a \"(__${fn}_complete)\"\n`}function safeName(name){return name.replace(/[^a-zA-Z0-9]/g,\"_\")}function defineSubcommand(def){return def}function defineCommand(def){return def}function typedWith(){return{defineCommand(def){return def},defineSubcommand(def){return def}}}function createCli(config,commands=[]){return{async run(argv=process.argv.slice(2)){if(argv.some((a)=>a===\"--version\")){if(config.version){print(`${config.name} ${config.version}`)}return}const userCommands=await resolveCommands(config,commands);const builtins=buildBuiltins(config);const allCommands=mergeByName(userCommands,builtins);if(argv[0]===\"__complete\"){const cword=parseInt(argv[1]??\"0\",10);const words=argv.slice(2);const partial=words[cword]??\"\";const prevArgv=words.slice(0,cword);const results=await resolveCompletions(allCommands,prevArgv,partial,config.name);if(results.length>0)process.stdout.write(results.join(`\n`)+`\n`);return}await dispatch(config,allCommands,argv)}}}var RESERVED_NAMES=new Set([\"plugin\"]);async function resolveCommands(config,staticCommands){for(const cmd of staticCommands){if(RESERVED_NAMES.has(cmd.name)){console.warn(`[cape] Warning: \"${cmd.name}\" is a reserved built-in command name and will shadow the built-in.`)}}const staticNames=new Set(staticCommands.map((c)=>c.name));const frameworkCfg=await readFrameworkConfig(config.name);const tomlPluginDirs=frameworkCfg[\"pluginDirs\"]??[];const dirs=defaultPluginDirs(config.name).concat(config.pluginDirs??[]).concat(tomlPluginDirs);const discovered=await discoverPlugins(dirs);const pluginCommands=[];for(const plugin of discovered){if(staticNames.has(plugin.manifest.name))continue;try{const cmd=await loadPlugin(plugin,\"run\");pluginCommands.push(cmd)}catch(err){console.warn(`[cape] Failed to load plugin \"${plugin.manifest.name}\": ${err}`)}}const takenNames=new Set([...staticNames,...pluginCommands.map((c)=>c.name)]);const builtins=createBuiltinCommands(config.name,config.pluginDirs??[],config.version??\"0.0.0\",config.config??{}).filter((b)=>!takenNames.has(b.name));return[...staticCommands,...pluginCommands,...builtins]}function defaultPluginDirs(cliName){return[join8(process.cwd(),\"commands\"),join8(homedir3(),\".config\",cliName,\"plugins\")]}async function dispatch(config,commands,argv){const tokens=tokenize(argv);const{commandName,subcommandName,strippedArgv}=extractCommandTokens(argv,commands);if(!commandName){try{const parsed2=resolve(tokens,globalSchema);const globals2=extractGlobalFlags(parsed2);if(globals2.help){print(renderHelp(config,{level:\"root\",commands:toSummaries(commands)},{noColor:globals2.noColor}));return}}catch{}print(renderHelp(config,{level:\"root\",commands:toSummaries(commands)}));return}const command=findByName2(commands,commandName);if(!command){const suggestion=closestName(commandName,commands.map((c)=>c.name));printError(`Error: unknown command \"${commandName}\"${suggestion?` \\u2014 did you mean \"${suggestion}\"?`:\"\"}`);printError(`Run '${config.name} --help' to see available commands.`);process.exit(2)}const cmdSchema=command.schema??{};const subcommands=command.subcommands??[];const subcommand=subcommandName?findByName2(subcommands,subcommandName):undefined;if(subcommandName&&!subcommand){try{const merged2=mergeSchemas(globalSchema,cmdSchema);const parsed2=resolve(tokenize(strippedArgv),merged2);const globals2=extractGlobalFlags(parsed2);if(globals2.help){print(renderHelp(config,{level:\"command\",command:{name:command.name,description:command.description,schema:cmdSchema},subcommands:toSummaries(subcommands)},{noColor:globals2.noColor}));return}}catch{}const suggestion=closestName(subcommandName,subcommands.map((s)=>s.name));printError(`Error: unknown subcommand \"${subcommandName}\"${suggestion?` \\u2014 did you mean \"${suggestion}\"?`:\"\"}`);printError(`Run '${config.name} ${command.name} --help' to see available subcommands.`);process.exit(2)}const subSchema=subcommand?.schema??{};const merged=subcommand?mergeSchemas(mergeSchemas(globalSchema,cmdSchema),subSchema):mergeSchemas(globalSchema,cmdSchema);if(hasHelpFlag(strippedArgv)){const noColor=strippedArgv.includes(\"--no-color\");if(subcommand){print(renderHelp(config,{level:\"subcommand\",command:{name:command.name,description:command.description,schema:cmdSchema},subcommand:{name:subcommand.name,description:subcommand.description,schema:subSchema}},{noColor}))}else{print(renderHelp(config,{level:\"command\",command:{name:command.name,description:command.description,schema:cmdSchema},subcommands:toSummaries(subcommands)},{noColor}))}return}let finalArgv=strippedArgv;if(process.stdin.isTTY){try{const partial=resolve(tokenize(strippedArgv),merged,{skipRequired:true});const prompted=await fromSchema(merged,partial.provided);const extra=promptedToArgv(prompted);if(extra.length>0)finalArgv=[...strippedArgv,...extra]}catch(err){if(err instanceof PromptCancelledError){process.exit(130)}if(!(err instanceof NonTtyError))throw err}}let parsed;try{parsed=resolve(tokenize(finalArgv),merged)}catch(err){if(err instanceof ParseError){printError(`Error: ${err.message}`);if(err.suggestion)printError(`       ${err.suggestion}`);const helpCmd=subcommand?`${config.name} ${command.name} ${subcommand.name}`:`${config.name} ${command.name}`;printError(`Run '${helpCmd} --help' to see available flags.`);process.exit(2)}throw err}const globals=extractGlobalFlags(parsed);const activeSchema=subcommand?.schema??command.schema;const runtimeOpts={args:parsed,rawEnv:getEnv(),globals,cliName:config.name,commandName:subcommand?`${command.name}/${subcommand.name}`:command.name,schema:activeSchema};const runtime=new BasicRuntime(runtimeOpts);const commandConfigSchema=subcommand?{...command.config??{},...subcommand.config??{}}:command.config;await runtime.loadConfig(config.name,command.name,{overridePath:globals.config,cliSchema:config.config,commandSchema:commandConfigSchema});try{if(subcommand){await subcommand.run(parsed,runtime)}else if(command.run){await command.run(parsed,runtime)}else{print(renderHelp(config,{level:\"command\",command:{name:command.name,description:command.description,schema:cmdSchema},subcommands:toSummaries(subcommands)}))}runtime.flushOutput()}catch(err){if(err instanceof PromptCancelledError){process.exit(130)}throw err}finally{runtime.teardown()}}function buildBuiltins(config){return[initCommand(config),completionsCommand(config)]}function completionsCommand(config){const shellFlag={type:\"string\",description:\"Shell type: bash, zsh, or fish (default: auto-detect from $SHELL)\",complete:{type:\"static\",values:[\"bash\",\"zsh\",\"fish\"]}};return{name:\"completions\",description:\"Manage shell tab completions\",subcommands:[{name:\"generate\",description:\"Print the completion script for your shell to stdout\",schema:{flags:{shell:shellFlag}},async run(args,runtime){const shell=resolveShellArg(args.flags[\"shell\"],runtime);if(!shell)return;runtime.print(generateCompletionScript(config.name,shell))}},{name:\"install\",description:\"Install tab completions for your shell\",schema:{flags:{shell:shellFlag}},async run(args,runtime){const shell=resolveShellArg(args.flags[\"shell\"],runtime);if(!shell)return;const script=generateCompletionScript(config.name,shell);const destPath=completionInstallPath(config.name,shell);const dir=destPath.slice(0,destPath.lastIndexOf(\"/\"));await mkdir3(dir,{recursive:true});await Bun.write(destPath,script);runtime.print(postInstallMessage(config.name,shell,destPath))}}]}}function initCommand(config){return{name:\"init\",description:`Set up ${config.displayName??config.name}: configure credentials and install shell completions`,async run(_args,runtime){if(!process.stdin.isTTY){runtime.printError(\"Error: 'init' requires an interactive terminal.\");runtime.exit(1)}runtime.print(`Welcome to ${config.displayName??config.name}!`);const secrets=config.setup?.secrets??[];if(secrets.length>0){runtime.print(\"\");runtime.print(\"Configuring credentials...\");const credPath=runtime.fs.configPath(\"credentials.toml\");let doc={};if(await runtime.fs.exists(credPath)){try{doc=parseToml(await runtime.fs.read(credPath))}catch{}}for(const secret of secrets){const existing=doc[secret.key];if(secret.description)runtime.print(`  ${secret.description}`);let value;try{value=await text({message:secret.message,default:existing??secret.default})}catch(err){if(err instanceof PromptCancelledError)throw err;throw err}doc[secret.key]=value}await runtime.fs.write(credPath,serializeToml(doc),384);runtime.output.success(\"Credentials saved.\")}runtime.print(\"\");const shell=detectShell();if(shell){try{const script=generateCompletionScript(config.name,shell);const destPath=completionInstallPath(config.name,shell);const dir=destPath.slice(0,destPath.lastIndexOf(\"/\"));await mkdir3(dir,{recursive:true});await Bun.write(destPath,script);runtime.print(postInstallMessage(config.name,shell,destPath))}catch{runtime.printError(`Note: could not install completions automatically.`);runtime.printError(`Run '${config.name} completions install' to set them up manually.`)}}else{runtime.print(`Run '${config.name} completions install' to enable tab completions.`)}runtime.print(\"\");runtime.output.success(`${config.displayName??config.name} is ready. Run '${config.name} --help' to get started.`)}}}function resolveShellArg(raw,runtime){const detected=detectShell();const shell=raw??detected;if(!shell){runtime.printError(\"Error: could not detect shell. Pass --shell bash, --shell zsh, or --shell fish.\");return}if(![\"bash\",\"zsh\",\"fish\"].includes(shell)){runtime.printError(`Error: unsupported shell \"${shell}\". Use bash, zsh, or fish.`);return}return shell}function mergeByName(primary,fallback){const names=new Set(primary.map((c)=>c.name));return[...primary,...fallback.filter((c)=>!names.has(c.name))]}function extractCommandTokens(argv,commands){const cmdResult=schemaAwareFreeValue(argv,0,globalSchema);if(!cmdResult){return{commandName:undefined,subcommandName:undefined,strippedArgv:argv}}const commandName=cmdResult.value;const command=findByName2(commands,commandName);let subcommandName;let subIdx=-1;if(command&&(command.subcommands??[]).length>0){const scanSchema=mergeSchemas(globalSchema,command.schema??{});const subResult=schemaAwareFreeValue(argv,cmdResult.index+1,scanSchema);if(subResult){subcommandName=subResult.value;subIdx=subResult.index}}const strippedArgv=argv.filter((_,i)=>i!==cmdResult.index&&i!==subIdx);return{commandName,subcommandName,strippedArgv}}function schemaAwareFreeValue(argv,startIndex,schema){const flags=schema.flags??{};const aliasMap=new Map;for(const[name,def]of Object.entries(flags)){if(def.alias)aliasMap.set(`-${def.alias}`,name)}let i=startIndex;while(i<argv.length){const arg=argv[i];if(arg===\"--\")break;if(arg.startsWith(\"-\")){if(arg.includes(\"=\")){i++;continue}const canonical=arg.startsWith(\"--\")?arg.slice(2):aliasMap.get(arg);const def=canonical!==undefined?flags[canonical]:undefined;if(def){i+=def.type!==\"boolean\"?2:1}else{const next=argv[i+1];i+=next&&!next.startsWith(\"-\")&&next!==\"--\"?2:1}continue}return{value:arg,index:i}}return}function findByName2(items,name){return items.find((item)=>item.name===name||(item.aliases??[]).includes(name))}function toSummaries(items){return items.map(({name,aliases,description})=>({name,aliases,description}))}function closestName(input,names){let best;let bestDist=Infinity;for(const name of names){const d=editDistance2(input,name);if(d<bestDist){bestDist=d;best=name}}return bestDist<=2?best:undefined}function editDistance2(a,b){const m=a.length,n=b.length;const dp=Array.from({length:m+1},(_,i)=>Array.from({length:n+1},(_2,j)=>i===0?j:j===0?i:0));for(let r=1;r<=m;r++)for(let c=1;c<=n;c++)dp[r][c]=a[r-1]===b[c-1]?dp[r-1][c-1]:1+Math.min(dp[r-1][c],dp[r][c-1],dp[r-1][c-1]);return dp[m][n]}function getEnv(){return Object.fromEntries(Object.entries(process.env).filter((e)=>e[1]!==undefined))}function hasHelpFlag(argv){for(const arg of argv){if(arg===\"--\")break;if(arg===\"--help\"||arg===\"-h\")return true}return false}function print(text2){process.stdout.write(text2+`\n`)}function printError(text2){process.stderr.write(text2+`\n`)}export{typedWith,tokenize,text,select,resolveCompletions,resolve,renderHelp,promptedToArgv,multiSelect,mergeSchemas,loadPlugin,globalSchema,fromSchema,extractGlobalFlags,executionMode,discoverPlugins,defineSubcommand,defineConfigSchema,defineConfig,defineCommand,createCli,confirm,autocomplete,PromptCancelledError,ParseError,NonTtyError,MockRuntime,MockExitError,HttpError,FRAMEWORK_VERSION};\n";

/** Cape public API type declarations written to node_modules/cape/index.d.ts */
export const CAPE_TYPES = "/**\n * Cape — CLI Application Plugin Engine\n * Public API type declarations.\n *\n * Auto-generated from source by prebuild.ts. Do not edit.\n * Run `bun run cape:prebuild` to regenerate.\n */\n\n/**\n * Config schema declared on a `CliConfig` (top-level keys) or a `CommandDef`\n * (command-scoped section).  Keys map to their field definitions.\n */\nexport type ConfigSchema = Record<string, ConfigField>;\n/** A scalar (string / number / boolean) config field. */\nexport interface ConfigScalarField {\n    type: \"string\" | \"number\" | \"boolean\";\n    description?: string;\n    default?: string | number | boolean;\n}\n/** An array config field whose items are described by a nested ConfigField. */\nexport interface ConfigArrayField {\n    type: \"array\";\n    /** Schema for each element — any ConfigField variant, including object. */\n    items: ConfigField;\n    description?: string;\n    default?: unknown[];\n}\n/** A nested-object config field whose sub-keys are declared in `fields`. */\nexport interface ConfigObjectField {\n    type: \"object\";\n    description?: string;\n    fields: ConfigSchema;\n}\n/**\n * A single config file key — a scalar value, an array, or a nested object.\n * Discriminated on `type`.\n */\nexport type ConfigField = ConfigScalarField | ConfigArrayField | ConfigObjectField;\n/** Context passed to dynamic completion fetchers. */\nexport interface CompletionCtx {\n    /** The partial word being completed. */\n    partial: string;\n    /** Flag values already typed on the command line (best-effort, may be incomplete). */\n    flags: Record<string, unknown>;\n}\nexport type CompletionSource = {\n    type: \"static\";\n    values: string[];\n} | {\n    type: \"dynamic\";\n    fetch: (ctx: CompletionCtx) => Promise<string[]>;\n    /** How long to cache results (ms). Omit to disable caching. */\n    cacheMs?: number;\n    /** Abort and return [] if fetch takes longer than this (ms). Default: 5000. */\n    timeoutMs?: number;\n    /** Other flag names whose values should be available in ctx.flags before fetching. */\n    dependsOn?: string[];\n};\nexport interface ArgSchema {\n    positionals?: {\n        name: string;\n        variadic?: boolean;\n        complete?: CompletionSource;\n    }[];\n    flags?: {\n        [name: string]: {\n            type: \"boolean\" | \"string\" | \"number\";\n            alias?: string;\n            required?: boolean;\n            multiple?: boolean;\n            default?: unknown;\n            description?: string;\n            complete?: CompletionSource;\n            hideInSubcommandHelp?: boolean;\n        };\n    };\n    /**\n     * Environment variable names to expose on `runtime.env`.\n     * When declared, only these variables are visible to the command.\n     * When omitted, all environment variables are passed through.\n     */\n    env?: string[];\n}\nexport interface ParsedArgs {\n    flags: Record<string, unknown>;\n    positionals: string[];\n    passthrough: string[];\n    /** Flag names that were explicitly set by the user (excludes defaults). */\n    provided: Set<string>;\n}\nexport type TokenType = \"flag\" | \"value\" | \"separator\";\nexport interface Token {\n    type: TokenType;\n    raw: string;\n}\n\n/**\n * Converts raw argv into a flat token stream.\n *\n * Rules:\n * - \"--\" → separator token; all subsequent args become values (passthrough)\n * - \"--flag=value\" → two tokens: flag \"--flag\", value \"value\"\n * - \"--flag\" / \"-f\" → flag token\n * - \"-abc\" → cluster, expanded to flag tokens \"-a\", \"-b\", \"-c\"\n * - anything else → value token\n */\nexport declare function tokenize(argv: string[]): Token[];\n\nexport declare class ParseError extends Error {\n    readonly suggestion?: string | undefined;\n    constructor(message: string, suggestion?: string | undefined);\n}\nexport interface ResolveOptions {\n    /**\n     * When true, skips the required-flag and required-positional checks.\n     * Useful for partial parsing (e.g. before interactive prompting fills in\n     * missing required values).\n     */\n    skipRequired?: boolean;\n}\nexport declare function resolve(tokens: Token[], schema: ArgSchema, opts?: ResolveOptions): ParsedArgs;\n\n/** The framework's built-in flags, available on every command. */\nexport declare const globalSchema: ArgSchema;\nexport interface GlobalFlags {\n    help: boolean;\n    version: boolean;\n    json: boolean;\n    noColor: boolean;\n    quiet: boolean;\n    verbose: boolean;\n    debug: boolean;\n    config: string | undefined;\n}\n/**\n * Merges two schemas. `overlay` wins on flag name collision (command flags\n * shadow global flags). Positionals come from `overlay` only — global flags\n * never declare positionals.\n */\nexport declare function mergeSchemas(base: ArgSchema, overlay: ArgSchema): ArgSchema;\n/**\n * Extracts typed global flag values from a parsed args object.\n * If `--debug` is set, `verbose` is also considered active.\n */\nexport declare function extractGlobalFlags(parsed: ParsedArgs): GlobalFlags;\n\nexport interface TableOptions {\n    /** Explicit column order. Defaults to keys of the first row. */\n    columns?: string[];\n}\nexport interface Spinner {\n    /** Update the spinner message while it's running. */\n    update(message: string): void;\n    /** Stop and print a success line. */\n    succeed(message?: string): void;\n    /** Stop and print a failure line. */\n    fail(message?: string): void;\n    /** Stop and erase the spinner without printing anything. */\n    stop(): void;\n}\nexport interface ProgressBar {\n    /** Advance by n (default 1). */\n    tick(n?: number): void;\n    /** Update the total. */\n    setTotal(total: number): void;\n    /** Mark complete and optionally print a final message. */\n    done(message?: string): void;\n}\nexport interface OutputInterface {\n    /** Raw text line — same as runtime.print(). */\n    print(text: string): void;\n    /** Raw error line — same as runtime.printError(). */\n    printError(text: string): void;\n    /** Green checkmark + message. Silent when --quiet. */\n    success(message: string): void;\n    /** Yellow warning + message. Always shown. */\n    warn(message: string): void;\n    /** Emit `value` as pretty-printed JSON. Bypasses --quiet. */\n    json(value: unknown): void;\n    /**\n     * Render an array of row objects as a table.\n     * TTY → box-drawing table. Pipe → tab-separated values.\n     */\n    table(rows: Record<string, unknown>[], opts?: TableOptions): void;\n    /** Render a list of strings. TTY → bullets, pipe → plain lines. */\n    list(items: string[]): void;\n    /** Start a spinner with an initial message. */\n    spinner(message: string): Spinner;\n    /** Run `fn`, show a spinner while it executes, then stop. */\n    withSpinner<T>(message: string, fn: (spinner: Spinner) => Promise<T>): Promise<T>;\n    /** Create a progress bar with the given total. */\n    progressBar(total: number): ProgressBar;\n    /** Run `fn` with a tick callback, show progress while it executes. */\n    withProgressBar<T>(total: number, fn: (tick: (n?: number) => void) => Promise<T>): Promise<T>;\n}\ninterface JsonOutputInternal extends OutputInterface {\n    /** Flush the accumulated output as a single JSON value to stdout. */\n    flushJson(): void;\n}\n/**\n * Creates an output that accumulates calls and emits a single JSON object.\n *\n * Flush shape:\n *   - texts=[], results=[v]         → v  (unwrapped single value)\n *   - texts=[], results=[v1,v2]     → { results: [v1, v2] }\n *   - texts=[t], results=[]         → { output: [t] }\n *   - texts=[t], results=[v]        → { output: [t], results: [v] }\n *\n * Texts are suppressed when quiet=true (results still captured).\n * printError / warn always write to stderr (not captured).\n */\nexport declare function createJsonOutput(quiet: boolean): JsonOutputInternal;\ninterface OutputOptions {\n    noColor: boolean;\n    quiet: boolean;\n    /** Whether stdout is a TTY (for table/spinner/progress rendering). */\n    isTTY: boolean;\n}\nexport declare function createOutput(opts: OutputOptions): OutputInterface;\nexport type OutputCall = {\n    type: \"print\";\n    text: string;\n} | {\n    type: \"printError\";\n    text: string;\n} | {\n    type: \"success\";\n    message: string;\n} | {\n    type: \"warn\";\n    message: string;\n} | {\n    type: \"json\";\n    value: unknown;\n} | {\n    type: \"table\";\n    rows: Record<string, unknown>[];\n    opts?: TableOptions;\n} | {\n    type: \"list\";\n    items: string[];\n} | {\n    type: \"spinner\";\n    message: string;\n} | {\n    type: \"progressBar\";\n    total: number;\n};\n/** Mock OutputInterface that records all calls for use in tests. */\nexport declare function createMockOutput(): OutputInterface & {\n    calls: OutputCall[];\n};\nexport {};\n\nexport interface FsInterface {\n    /** Read a file as a UTF-8 string. */\n    read(path: string): Promise<string>;\n    /** Read a file as raw bytes. */\n    readBytes(path: string): Promise<Uint8Array>;\n    /**\n     * Write a file.  Parent directory is created automatically.\n     * @param mode Optional Unix permissions, e.g. 0o600. Defaults to 0o644.\n     */\n    write(path: string, content: string | Uint8Array, mode?: number): Promise<void>;\n    /** Returns true if the path exists (file or directory). */\n    exists(path: string): Promise<boolean>;\n    /** List entries in a directory. Returns basenames only. */\n    list(path: string): Promise<string[]>;\n    /** Path under `$XDG_CONFIG_HOME/<cliName>` (or `~/.config/<cliName>`). */\n    configPath(...segments: string[]): string;\n    /** Path under `$XDG_DATA_HOME/<cliName>` (or `~/.local/share/<cliName>`). */\n    dataPath(...segments: string[]): string;\n    /** Path under `$XDG_CACHE_HOME/<cliName>` (or `~/.cache/<cliName>`). */\n    cachePath(...segments: string[]): string;\n}\nexport declare function createFs(cliName: string): FsInterface;\nexport type MockFsEntry = string | Uint8Array;\n/** Mock FsInterface backed by an in-memory Map. */\nexport declare function createMockFs(cliName?: string): FsInterface & {\n    files: Map<string, MockFsEntry>;\n};\nexport declare function xdgConfigHome(): string;\nexport declare function xdgDataHome(): string;\nexport declare function xdgCacheHome(): string;\n/** Expands a leading `~/` or bare `~` to the user's home directory. */\nexport declare function expandHome(p: string): string;\n\nexport interface StdinInterface {\n    /** True when stdin is connected to a terminal. */\n    readonly isTTY: boolean;\n    /**\n     * Read all of stdin and return it as a string.\n     * On a TTY this reads until the user presses Ctrl+D.\n     */\n    read(): Promise<string>;\n    /**\n     * Async iterable of lines from stdin.\n     * Each value is a line without the trailing newline.\n     */\n    lines(): AsyncIterable<string>;\n}\nexport declare function createStdin(): StdinInterface;\n/** Mock StdinInterface that reads from a pre-set string. */\nexport declare function createMockStdin(content?: string, isTTY?: boolean): StdinInterface;\n\nexport interface LogInterface {\n    /**\n     * Log a verbose message.\n     * Only printed when the user passed `--verbose` or `--debug`.\n     */\n    verbose(message: string): void;\n    /**\n     * Log a debug message.\n     * Only printed when the user passed `--debug`.\n     */\n    debug(message: string): void;\n}\ninterface LogOptions {\n    verbose: boolean;\n    debug: boolean;\n    noColor: boolean;\n}\nexport declare function createLog(opts: LogOptions): LogInterface;\nexport type LogCall = {\n    level: \"verbose\" | \"debug\";\n    message: string;\n};\n/** Mock LogInterface that records all log calls for use in tests. */\nexport declare function createMockLog(): LogInterface & {\n    calls: LogCall[];\n};\nexport {};\n\nexport interface SecretsInterface {\n    /** Get a secret value scoped to the current command. */\n    get(key: string): Promise<string | undefined>;\n    /** Set a secret value scoped to the current command. */\n    set(key: string, value: string): Promise<void>;\n    /** Delete a secret value scoped to the current command. */\n    delete(key: string): Promise<void>;\n}\nexport declare function createSecrets(cliName: string, commandName: string): SecretsInterface;\n/** Mock SecretsInterface backed by an in-memory Map. */\nexport declare function createMockSecrets(initial?: Record<string, string>): SecretsInterface & {\n    store: Map<string, string>;\n};\n\nexport type Key = {\n    type: \"char\";\n    char: string;\n} | {\n    type: \"up\";\n} | {\n    type: \"down\";\n} | {\n    type: \"left\";\n} | {\n    type: \"right\";\n} | {\n    type: \"enter\";\n} | {\n    type: \"backspace\";\n} | {\n    type: \"delete\";\n} | {\n    type: \"home\";\n} | {\n    type: \"end\";\n} | {\n    type: \"tab\";\n} | {\n    type: \"escape\";\n} | {\n    type: \"interrupt\";\n};\n/** Thrown when a prompt is invoked but stdin is not a TTY. */\nexport declare class NonTtyError extends Error {\n    constructor(message?: string);\n}\n/** Thrown when the user cancels a prompt (Ctrl+C or Escape). */\nexport declare class PromptCancelledError extends Error {\n    constructor();\n}\nexport interface TextPromptOptions {\n    message: string;\n    /** Pre-filled default value shown in the input. */\n    default?: string;\n    /** Return an error message string to reject the value, or undefined to accept. */\n    validate?: (value: string) => string | undefined;\n    signal?: AbortSignal;\n}\nexport interface SelectPromptOptions {\n    message: string;\n    choices: string[];\n    /** Initially highlighted choice. */\n    default?: string;\n    signal?: AbortSignal;\n}\nexport interface ConfirmPromptOptions {\n    message: string;\n    /** If true, Enter accepts `true`; if false, Enter accepts `false`. Default: false. */\n    default?: boolean;\n    signal?: AbortSignal;\n}\nexport interface MultiSelectPromptOptions {\n    message: string;\n    choices: string[];\n    /** Pre-checked choices. */\n    defaults?: string[];\n    signal?: AbortSignal;\n}\nexport interface AutocompletePromptOptions {\n    message: string;\n    /**\n     * Static array of choices filtered locally, or an async function called on\n     * each query change with debouncing. The function receives the current query\n     * and an AbortSignal (aborted when a new query supersedes this one).\n     */\n    choices: string[] | ((query: string, signal: AbortSignal) => Promise<string[]>);\n    default?: string;\n    /** Debounce delay for dynamic fetchers (ms). Default: 150. */\n    debounceMs?: number;\n    signal?: AbortSignal;\n}\n/**\n * All interactive prompt methods, pre-bound to the command's AbortSignal.\n * Access via `runtime.prompt` — no separate imports needed.\n *\n * All methods throw NonTtyError when stdin is not a TTY, and\n * PromptCancelledError when the user presses Ctrl+C or Escape.\n */\nexport interface PromptInterface {\n    /** Free-form text input. */\n    text(options: Omit<TextPromptOptions, \"signal\">): Promise<string>;\n    /** Yes/no confirmation. */\n    confirm(options: Omit<ConfirmPromptOptions, \"signal\">): Promise<boolean>;\n    /** Single choice from a list. */\n    select(options: Omit<SelectPromptOptions, \"signal\">): Promise<string>;\n    /** Multiple choices from a list. */\n    multiSelect(options: Omit<MultiSelectPromptOptions, \"signal\">): Promise<string[]>;\n    /** Text input with live-filtered suggestions (static list or async fetch). */\n    autocomplete(options: Omit<AutocompletePromptOptions, \"signal\">): Promise<string>;\n    /** Thrown when a prompt is invoked outside a TTY. */\n    NonTtyError: typeof NonTtyError;\n    /** Thrown when the user cancels a prompt (Ctrl+C or Escape). */\n    PromptCancelledError: typeof PromptCancelledError;\n}\n\n/** Thrown by runtime.http convenience methods on non-2xx responses. */\nexport declare class HttpError extends Error {\n    readonly status: number;\n    readonly statusText: string;\n    readonly url: string;\n    /** Raw response body text. */\n    readonly body: string;\n    constructor(status: number, statusText: string, url: string, \n    /** Raw response body text. */\n    body: string);\n}\nexport interface HttpRequestOptions {\n    /** Additional headers merged with defaults. */\n    headers?: Record<string, string>;\n}\nexport interface HttpInterface {\n    /**\n     * GET the URL and parse the response as JSON.\n     * Throws HttpError on non-2xx.\n     */\n    get<T = unknown>(url: string, options?: HttpRequestOptions): Promise<T>;\n    /**\n     * POST the URL with a JSON-serialised body and parse the response as JSON.\n     * Throws HttpError on non-2xx.\n     */\n    post<T = unknown>(url: string, body?: unknown, options?: HttpRequestOptions): Promise<T>;\n    /**\n     * PUT the URL with a JSON-serialised body and parse the response as JSON.\n     * Throws HttpError on non-2xx.\n     */\n    put<T = unknown>(url: string, body?: unknown, options?: HttpRequestOptions): Promise<T>;\n    /**\n     * PATCH the URL with a JSON-serialised body and parse the response as JSON.\n     * Throws HttpError on non-2xx.\n     */\n    patch<T = unknown>(url: string, body?: unknown, options?: HttpRequestOptions): Promise<T>;\n    /**\n     * DELETE the URL and parse the response as JSON.\n     * Throws HttpError on non-2xx.\n     */\n    delete<T = unknown>(url: string, options?: HttpRequestOptions): Promise<T>;\n    /**\n     * Raw fetch — uses runtime.signal for cancellation but gives full control.\n     * Unlike the convenience methods, does NOT throw on non-2xx.\n     */\n    fetch(url: string, init?: Omit<RequestInit, \"signal\">): Promise<Response>;\n    /** The error class thrown on non-2xx responses. */\n    HttpError: typeof HttpError;\n}\nexport declare function createHttp(signal: AbortSignal): HttpInterface;\nexport interface MockHttpCall {\n    method: string;\n    url: string;\n    body?: unknown;\n}\nexport declare function createMockHttp(): HttpInterface & {\n    /** All requests made, in order. */\n    calls: MockHttpCall[];\n    /** Pre-configure responses: url → value returned from convenience methods. */\n    mockJson(url: string, response: unknown): void;\n};\n\nexport interface Runtime {\n    /** Parsed args for the current command invocation. */\n    args: ParsedArgs;\n    /**\n     * Environment variables available to this command.\n     * When the command schema declares `env: [\"FOO\", \"BAR\"]`, only those\n     * variables are exposed.  Otherwise all env vars pass through.\n     */\n    env: Record<string, string>;\n    /** Write a line to stdout. */\n    print(text: string): void;\n    /** Write a line to stderr. */\n    printError(text: string): void;\n    /** Exit the process with the given exit code. */\n    exit(code: number): never;\n    /** Rich output interface — table, list, json, success, warn, spinner, progress. */\n    output: OutputInterface;\n    /** Filesystem helpers with XDG path utilities. */\n    fs: FsInterface;\n    /** Stdin access — isTTY check, read, line iteration. */\n    stdin: StdinInterface;\n    /** Verbose / debug logging wired to --verbose / --debug global flags. */\n    log: LogInterface;\n    /**\n     * AbortSignal that is aborted when SIGINT or SIGTERM is received.\n     * Pass this to fetch(), long-running loops, or async operations so they\n     * can cancel cleanly.\n     */\n    signal: AbortSignal;\n    /**\n     * Register a cleanup function that runs when the process receives SIGINT or\n     * SIGTERM.  Handlers run in reverse registration order (last-in, first-out).\n     */\n    onExit(fn: () => void | Promise<void>): void;\n    /** Secrets interface — get/set/delete values from credentials.toml. */\n    secrets: SecretsInterface;\n    /** Top-level config from `~/.config/<cli>/config.toml`. */\n    config: Record<string, unknown>;\n    /** Command-specific section from config.toml (`[command-name]`). */\n    commandConfig: Record<string, unknown>;\n    /**\n     * Interactive prompt helpers — pre-bound to this command's AbortSignal.\n     * No separate imports needed; use `runtime.prompt.text(...)` etc.\n     */\n    prompt: PromptInterface;\n    /**\n     * HTTP client — pre-bound to this command's AbortSignal.\n     * Convenience methods parse JSON and throw HttpError on non-2xx.\n     * Use `runtime.http.fetch(...)` for full control.\n     */\n    http: HttpInterface;\n}\n\ninterface MockRuntimeOptions {\n    args?: Partial<ParsedArgs>;\n    env?: Record<string, string>;\n    stdinContent?: string;\n    stdinIsTTY?: boolean;\n    secrets?: Record<string, string>;\n    config?: Record<string, unknown>;\n    commandConfig?: Record<string, unknown>;\n    /** Virtual filesystem entries: path → content. */\n    files?: Record<string, MockFsEntry>;\n    cliName?: string;\n}\nexport declare class MockRuntime implements Runtime {\n    args: ParsedArgs;\n    env: Record<string, string>;\n    output: OutputInterface;\n    fs: FsInterface;\n    stdin: StdinInterface;\n    log: LogInterface;\n    signal: AbortSignal;\n    secrets: SecretsInterface;\n    config: Record<string, unknown>;\n    commandConfig: Record<string, unknown>;\n    prompt: PromptInterface;\n    http: HttpInterface;\n    readonly printed: string[];\n    readonly errors: string[];\n    exitCode: number | undefined;\n    /** All output method calls, in order. */\n    get outputCalls(): OutputCall[];\n    /** All log calls. */\n    get logCalls(): LogCall[];\n    /** Secret store (readable in tests). */\n    get secretStore(): Map<string, string>;\n    /** Virtual filesystem (readable / writable in tests). */\n    get fsFiles(): Map<string, MockFsEntry>;\n    private _signalManager;\n    constructor(options?: MockRuntimeOptions);\n    print(text: string): void;\n    printError(text: string): void;\n    exit(code: number): never;\n    onExit(fn: () => void | Promise<void>): void;\n    /** Simulate receiving SIGINT / SIGTERM. Runs all registered onExit handlers. */\n    abort(): Promise<void>;\n}\nexport declare class MockExitError extends Error {\n    readonly code: number;\n    constructor(code: number);\n}\nexport {};\n\nexport interface CliInfo {\n    name: string;\n    displayName?: string;\n    version?: string;\n    description: string;\n}\nexport interface CommandSummary {\n    name: string;\n    aliases?: string[];\n    description: string;\n}\nexport interface CommandDetail {\n    name: string;\n    description: string;\n    schema: ArgSchema;\n}\nexport type HelpContext = {\n    level: \"root\";\n    commands: CommandSummary[];\n} | {\n    level: \"command\";\n    command: CommandDetail;\n    subcommands: CommandSummary[];\n} | {\n    level: \"subcommand\";\n    command: CommandDetail;\n    subcommand: CommandDetail;\n};\nexport interface RenderOptions {\n    noColor?: boolean;\n}\n\nexport declare function renderHelp(cli: CliInfo, ctx: HelpContext, opts?: RenderOptions): string;\n\n/** The shape of a *.plugin.toml file — cheap to read, no JS import needed. */\nexport interface MinimalManifest {\n    name: string;\n    description: string;\n    /** Path to the TS command file, relative to the *.plugin.toml location. */\n    command: string;\n    enabled: boolean;\n    frameworkVersion: string;\n}\n/** A plugin found on disk but not yet imported. */\nexport interface DiscoveredPlugin {\n    manifest: MinimalManifest;\n    /** Absolute path to the directory containing the *.plugin.toml file. */\n    pluginDir: string;\n    /** Absolute path to the *.plugin.toml file itself. */\n    manifestPath: string;\n}\n\nexport interface DiscoverOptions {\n    /** When true, disabled plugins are included in results. Default: false. */\n    includeDisabled?: boolean;\n}\n/**\n * Recursively scans `dirs` for *.plugin.toml files and returns all valid,\n * enabled plugins. Directories that don't exist are silently skipped.\n * Malformed manifests are warned about and skipped.\n */\nexport declare function discoverPlugins(dirs: string[], opts?: DiscoverOptions): Promise<DiscoveredPlugin[]>;\n\n/**\n * The shape of a single flag definition — mirrors ArgSchema flags entry\n * but kept explicit here so the inference types can reference it directly.\n */\nexport type FlagDef = {\n    type: \"boolean\" | \"string\" | \"number\";\n    alias?: string;\n    required?: boolean;\n    multiple?: boolean;\n    default?: unknown;\n    description?: string;\n    complete?: CompletionSource;\n    hideInSubcommandHelp?: boolean;\n};\n/** Maps \"boolean\" | \"string\" | \"number\" to their TypeScript types. */\ntype FlagBaseType<T extends \"boolean\" | \"string\" | \"number\"> = T extends \"boolean\" ? boolean : T extends \"string\" ? string : T extends \"number\" ? number : never;\n/**\n * Infers the TypeScript type of a single parsed flag value.\n *\n * - multiple: true         → array\n * - boolean               → always boolean (defaults to false, never undefined)\n * - required: true         → never undefined\n * - default provided       → never undefined\n * - otherwise              → value | undefined\n */\ntype InferFlagValue<F extends FlagDef> = F[\"multiple\"] extends true ? FlagBaseType<F[\"type\"]>[] : F[\"type\"] extends \"boolean\" ? boolean : F[\"required\"] extends true ? FlagBaseType<F[\"type\"]> : F extends {\n    default: NonNullable<unknown>;\n} ? FlagBaseType<F[\"type\"]> : FlagBaseType<F[\"type\"]> | undefined;\n/** Infers the typed flags record from a flags schema object. */\ntype InferFlags<F extends Record<string, FlagDef>> = {\n    [K in keyof F]: InferFlagValue<F[K]>;\n};\n/**\n * Infers a fully typed ParsedArgs from an ArgSchema.\n * Falls back to the untyped ParsedArgs if the flags shape can't be resolved.\n */\nexport type InferParsedArgs<S extends ArgSchema> = S[\"flags\"] extends Record<string, FlagDef> ? {\n    flags: InferFlags<S[\"flags\"]>;\n    positionals: string[];\n    passthrough: string[];\n} : ParsedArgs;\ntype ScalarType = \"string\" | \"number\" | \"boolean\";\ntype ConfigBaseType<T extends ScalarType> = T extends \"string\" ? string : T extends \"number\" ? number : T extends \"boolean\" ? boolean : never;\n/**\n * Infers the element type for an array field — same logic as InferConfigValue\n * but without the optional/required wrapping (the array itself carries that).\n * Supports scalars, objects, and nested arrays.\n */\ntype InferArrayItemType<F extends ConfigField> = F extends {\n    type: \"object\";\n    fields: infer FS extends ConfigSchema;\n} ? InferConfig<FS> : F extends {\n    type: \"array\";\n    items: infer I extends ConfigField;\n} ? InferArrayItemType<I>[] : F extends ConfigScalarField ? ConfigBaseType<F[\"type\"]> : never;\n/**\n * Infers the TypeScript type of a single config value.\n *\n * - type: \"object\"   → recursively infer nested ConfigSchema\n * - type: \"array\"    → typed array of the inferred item type\n * - default provided → value is always present (never undefined)\n * - no default       → value may be undefined (key absent from all config files)\n */\ntype InferConfigValue<F extends ConfigField> = F extends {\n    type: \"object\";\n    fields: infer FS extends ConfigSchema;\n} ? InferConfig<FS> : F extends {\n    type: \"array\";\n    items: infer I extends ConfigField;\n} ? InferArrayItemType<I>[] : F extends ConfigScalarField & {\n    default: NonNullable<unknown>;\n} ? ConfigBaseType<F[\"type\"]> : F extends ConfigScalarField ? ConfigBaseType<F[\"type\"]> | undefined : never;\n/**\n * Infers a typed config record from a ConfigSchema.\n * When the schema is empty (no config declared), falls back to\n * Record<string, unknown> so untyped access still compiles.\n */\nexport type InferConfig<CC extends ConfigSchema> = [\n    keyof CC\n] extends [never] ? Record<string, unknown> : {\n    [K in keyof CC]: InferConfigValue<CC[K]>;\n};\nexport {};\n\nexport interface SubcommandDef {\n    name: string;\n    aliases?: string[];\n    description: string;\n    schema?: ArgSchema;\n    /** Config schema for this subcommand's section in config.toml. */\n    config?: ConfigSchema;\n    run(args: ParsedArgs, runtime: Runtime): Promise<void>;\n}\nexport interface CommandDef {\n    name: string;\n    aliases?: string[];\n    description: string;\n    schema?: ArgSchema;\n    /** Config schema for this command's section in config.toml. */\n    config?: ConfigSchema;\n    subcommands?: SubcommandDef[];\n    run?(args: ParsedArgs, runtime: Runtime): Promise<void>;\n}\n/**\n * A Runtime with both `commandConfig` and `config` narrowed to their inferred types.\n * `CC` is the command-level config schema; `GC` is the CLI-level (global) config schema.\n */\ntype RuntimeWith<CC extends ConfigSchema, GC extends ConfigSchema> = Omit<Runtime, \"commandConfig\" | \"config\"> & {\n    commandConfig: InferConfig<CC>;\n    config: InferConfig<GC>;\n};\n/**\n * Defines a subcommand with schema-inferred arg types.\n * TypeScript captures the exact schema shape and types `args` in `run` accordingly —\n * no casts needed inside the implementation.\n */\nexport declare function defineSubcommand<S extends ArgSchema, CC extends ConfigSchema = Record<never, ConfigField>>(def: {\n    name: string;\n    aliases?: string[];\n    description: string;\n    schema?: S;\n    config?: CC;\n    run(args: InferParsedArgs<S>, runtime: RuntimeWith<CC, Record<never, ConfigField>>): Promise<void>;\n}): SubcommandDef;\n/**\n * Defines a command with schema-inferred arg types.\n * TypeScript captures the exact schema shape and types `args` in `run` accordingly —\n * no casts needed inside the implementation.\n */\nexport declare function defineCommand<S extends ArgSchema, CC extends ConfigSchema = Record<never, ConfigField>>(def: {\n    name: string;\n    aliases?: string[];\n    description: string;\n    schema?: S;\n    config?: CC;\n    subcommands?: SubcommandDef[];\n    run?(args: InferParsedArgs<S>, runtime: RuntimeWith<CC, Record<never, ConfigField>>): Promise<void>;\n}): CommandDef;\n/**\n * Creates typed `defineCommand` / `defineSubcommand` helpers pre-bound to the\n * CLI-level config schema `GC`, so `runtime.config` is fully typed without\n * repeating the schema on every command.\n *\n * @example\n * ```ts\n * // shared/_typed.ts\n * import type { myCliConfig } from \"../cli.config.ts\";\n * export const { defineCommand, defineSubcommand } =\n *   typedWith<typeof myCliConfig.config>();\n *\n * // commands/deploy.ts\n * import { defineCommand } from \"../shared/_typed.ts\";\n * export const deployCommand = defineCommand({\n *   name: \"deploy\",\n *   run(_args, runtime) {\n *     const url = runtime.config.apiUrl; // typed!\n *   },\n * });\n * ```\n */\nexport declare function typedWith<GC extends ConfigSchema>(): {\n    defineCommand<S extends ArgSchema, CC extends ConfigSchema = Record<never, ConfigField>>(def: {\n        name: string;\n        aliases?: string[];\n        description: string;\n        schema?: S;\n        config?: CC;\n        subcommands?: SubcommandDef[];\n        run?(args: InferParsedArgs<S>, runtime: RuntimeWith<CC, GC>): Promise<void>;\n    }): CommandDef;\n    defineSubcommand<S extends ArgSchema, CC extends ConfigSchema = Record<never, ConfigField>>(def: {\n        name: string;\n        aliases?: string[];\n        description: string;\n        schema?: S;\n        config?: CC;\n        run(args: InferParsedArgs<S>, runtime: RuntimeWith<CC, GC>): Promise<void>;\n    }): SubcommandDef;\n};\nexport interface SetupSecret {\n    /** Key written to credentials.toml. */\n    key: string;\n    /** Prompt message shown to the user. */\n    message: string;\n    /** Optional hint printed below the prompt. */\n    description?: string;\n    /** Default value pre-filled in the prompt. */\n    default?: string;\n}\n/**\n * Where and how to publish binaries for `cape build`'s install.sh generation.\n *\n * @example GitHub Releases\n * install: { type: \"github\", repo: \"myorg/myctl\" }\n *\n * @example Self-hosted\n * install: { type: \"custom\", url: \"https://cli.mycompany.com/releases/v{VERSION}\" }\n * // {VERSION} is replaced with the version string at build time.\n * // Binaries are expected at <url>/<name>-<os>-<arch>\n */\nexport type InstallConfig = {\n    type: \"github\";\n    repo: string;\n} | {\n    type: \"custom\";\n    url: string;\n};\nexport interface CliConfig extends CliInfo {\n    /**\n     * Schema for top-level keys in config.toml (and `.{name}.toml`).\n     * Declares types, descriptions, and defaults for `runtime.config`.\n     */\n    config?: ConfigSchema;\n    /**\n     * Additional directories to scan for *.plugin.toml files.\n     * The framework always scans ./commands/ and ~/.config/<name>/plugins/\n     * first — these dirs are appended after.\n     */\n    pluginDirs?: string[];\n    /**\n     * Binary distribution configuration for install.sh generation.\n     * Replaces the older `repository` shorthand.\n     */\n    install?: InstallConfig;\n    /**\n     * @deprecated Use `install: { type: \"github\", repo: \"owner/repo\" }` instead.\n     * Kept for backwards compatibility.\n     */\n    repository?: string;\n    /**\n     * First-run setup prompts for the built-in `init` command.\n     * Each entry shows a text prompt and writes the answer to credentials.toml.\n     */\n    setup?: {\n        secrets?: SetupSecret[];\n    };\n}\nexport declare function createCli(config: CliConfig, commands?: CommandDef[]): {\n    run(argv?: string[]): Promise<void>;\n};\nexport {};\n\nexport type ExecutionMode = \"run\" | \"complete\";\n/**\n * The mode the framework set before importing this plugin module.\n * Plugin authors can read this at module level to skip expensive imports\n * that are only needed during execution (not completion).\n *\n * @example\n * import { executionMode } from \"cape\";\n * const client = executionMode === \"run\" ? await import(\"./heavy-sdk.ts\") : null;\n */\nexport declare const executionMode: ExecutionMode;\n\n/** The framework's own version — used for plugin compatibility checks. */\nexport declare const FRAMEWORK_VERSION = \"1.0.0\";\n/**\n * Dynamically imports a plugin module and returns its CommandDef.\n * Sets the global execution mode before import so plugins can read it\n * at module level to skip expensive dependencies during completion.\n *\n * Throws if the plugin is incompatible (major version mismatch) or if\n * the module has no default export.\n */\nexport declare function loadPlugin(plugin: DiscoveredPlugin, mode: ExecutionMode): Promise<CommandDef>;\n\n/**\n * Given the tokens already on the command line (before the cursor) and the\n * partial word being completed, returns completion candidates.\n *\n * @param commands  All available commands (static + loaded plugins)\n * @param argv      Tokens already typed, NOT including the partial word\n * @param partial   The word currently being completed (may be \"\")\n * @param cliName   CLI name used to scope the on-disk cache directory\n */\nexport declare function resolveCompletions(commands: CommandDef[], argv: string[], partial: string, cliName?: string): Promise<string[]>;\n\nexport interface TextState {\n    value: string;\n    cursor: number;\n    /**\n     * When true the current value is the default being shown as a placeholder\n     * (rendered dimmed). The first printable keypress replaces it entirely;\n     * cursor movement / backspace switches to normal editing instead.\n     */\n    isPlaceholder: boolean;\n    error: string | undefined;\n    done: boolean;\n    cancelled: boolean;\n}\nexport declare function textReducer(state: TextState, key: Key, validate?: (v: string) => string | undefined): TextState;\nexport declare function text(opts: TextPromptOptions): Promise<string>;\n\nexport interface SelectState {\n    choices: string[];\n    index: number;\n    done: boolean;\n    cancelled: boolean;\n}\nexport declare function selectReducer(state: SelectState, key: Key): SelectState;\nexport declare function renderSelect(state: SelectState, opts: SelectPromptOptions): string;\nexport declare function select(opts: SelectPromptOptions): Promise<string>;\n\nexport interface ConfirmState {\n    answer: boolean | undefined;\n    done: boolean;\n    cancelled: boolean;\n}\nexport declare function confirmReducer(state: ConfirmState, key: Key, defaultValue: boolean): ConfirmState;\nexport declare function confirm(opts: ConfirmPromptOptions): Promise<boolean>;\n\nexport interface MultiSelectState {\n    choices: string[];\n    index: number;\n    checked: Set<number>;\n    done: boolean;\n    cancelled: boolean;\n}\nexport declare function multiSelectReducer(state: MultiSelectState, key: Key): MultiSelectState;\nexport declare function renderMultiSelect(state: MultiSelectState, opts: MultiSelectPromptOptions): string;\nexport declare function multiSelect(opts: MultiSelectPromptOptions): Promise<string[]>;\n\nexport interface AutocompleteState {\n    query: string;\n    queryCursor: number;\n    items: string[];\n    index: number;\n    loading: boolean;\n    done: boolean;\n    cancelled: boolean;\n    /** The default value — used as fallback on Enter when items is empty and query is \"\". */\n    defaultValue?: string;\n}\ntype AutocompleteAction = {\n    type: \"key\";\n    key: Key;\n} | {\n    type: \"items\";\n    items: string[];\n};\nexport declare function autocompleteReducer(state: AutocompleteState, action: AutocompleteAction): AutocompleteState;\nexport declare function renderAutocomplete(state: AutocompleteState, opts: AutocompletePromptOptions, \n/** Current spinner frame index — incremented externally while loading. */\nspinnerFrame?: number): string;\n/** The autocomplete runner is custom (not using runPromptLoop) because it\n * interleaves key events with async fetch results. */\nexport declare function autocomplete(opts: AutocompletePromptOptions): Promise<string>;\nexport {};\n\n/**\n * Walks an ArgSchema and interactively prompts for any required flags that\n * are not present in `provided`.\n *\n * Mapping rules:\n *   - boolean flags: skipped (required booleans are not meaningful to prompt for)\n *   - static completion source with ≤8 choices: select\n *   - static completion source with >8 choices: autocomplete\n *   - dynamic completion source: autocomplete with live fetch\n *   - no completion source: text\n *   - multiple flags with static source: multi-select\n *\n * Returns a plain object whose keys are flag names and values are the prompted\n * values (strings or string[] for multiple flags). Callers are responsible for\n * type coercion (e.g. string → number) before merging with parsed args.\n */\nexport declare function fromSchema(schema: ArgSchema, provided: Set<string>): Promise<Record<string, unknown>>;\n/**\n * Converts prompted values to argv tokens that can be appended to an existing\n * argv array and re-parsed through the normal resolve() path.\n *\n * E.g. `{ name: \"Alice\", count: 3 }` → `[\"--name\", \"Alice\", \"--count\", \"3\"]`\n * Arrays (multiple flags) produce one `--flag value` pair per element.\n */\nexport declare function promptedToArgv(prompted: Record<string, unknown>): string[];\n\n/**\n * The full configuration for a Cape-based CLI.\n *\n * Used in `cli.config.ts` in the project root. Pass it directly to\n * `createCli()` — the extra build-only fields (`entry`, `outfile`) are\n * silently ignored at runtime.\n */\nexport interface CliConfigDef extends CliConfig {\n    /**\n     * Semantic version string. Required — enables `--version` support.\n     * E.g. \"1.2.3\"\n     */\n    version: string;\n    /**\n     * Entry point file for `bun build --compile`.\n     * Default: \"./main.ts\" (resolved relative to cli.config.ts).\n     */\n    entry?: string;\n    /**\n     * Output binary filename. Default: `name`.\n     */\n    outfile?: string;\n}\n/**\n * Defines a CLI configuration with full TypeScript type checking.\n * Place this in `cli.config.ts` at your project root.\n *\n * @example\n * ```ts\n * // cli.config.ts\n * import { defineConfig } from \"cape\";\n *\n * export default defineConfig({\n *   name: \"myctl\",\n *   displayName: \"My Control\",\n *   version: \"1.0.0\",\n *   description: \"Manage your infrastructure\",\n * });\n * ```\n */\nexport declare function defineConfig<T extends CliConfigDef>(config: T): T;\n/**\n * Defines a config schema with preserved literal types — no `as const` needed.\n * Use this to declare the `globalConfig` in `cli.config.ts`.\n *\n * @example\n * ```ts\n * const globalConfig = defineConfigSchema({\n *   apiUrl: { type: \"string\", description: \"Base API URL\" },\n *   retries: { type: \"number\", default: 3 },\n *   tags:    { type: \"array\", items: { type: \"string\" } },\n * });\n * export const { defineCommand } = typedWith<typeof globalConfig>();\n * ```\n */\nexport declare function defineConfigSchema<S extends ConfigSchema>(schema: S): S;";
